# --------------------------------------------------------------------------- #
# RESERVED PACKAGE MARKER — `radar_chart`
# --------------------------------------------------------------------------- #
# Simple explanation:
# This file is an intentional placeholder. The active question-type radar
# (and its bar-chart fallback) lives in
# `app.dashboard.question_type_performance_chart`. This `radar_chart`
# package is kept as a reserved name so future dashboard work has an obvious
# home if a standalone radar component is ever needed.
#
# App connection:
# Nothing in the production dashboard imports from this module today.
