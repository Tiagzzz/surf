---
phase: 02-mock-taking-loop-p1-p5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .streamlit/config.toml
  - pyproject.toml
  - app/brain/theme/__init__.py
  - app/brain/theme/theme.py
  - app/brain/theme/theme.md
  - app/brain/theme/edit_this_later.md
  - app/mock_take/question_render/_difficulty.py
  - app/mock_take/question_render/_difficulty.md
  - assets/icons/star_filled.svg
  - assets/icons/star_empty.svg
  - previews/_theme.py
  - previews/_fixtures.py
  - previews/README.md
  - previews/components/_theme_bench/preview.py
  - previews/components/_theme_bench/_theme.py
  - previews/spikes/fragment_timer/preview.py
  - previews/spikes/card_interactive_overlay/preview.py
  - previews/spikes/SPIKES.md
  - tests/_streamlit_apptest_helpers.py
  - tests/test_theme_loader.py
  - tests/test_no_app_imports_in_previews.py
  - tests/test_db_connection_cache_resource.py
  - streamlit_app.py
  - ui/documentation.md
  - .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md
  - .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md
  - app/brain/claude_client/claude_client.md
  - app/brain/ingestion/pdf_to_md_v3.md
  - app/brain/ingestion/page_splitter/page_splitter.md
  - app/my_classes/factsheet_clean/factsheet_cleaner.md
  - app/my_classes/factsheet_clean/factsheet_renderer.md
  - app/class_/lo_extract/lo_extractor.md
  - app/class_/mcq_generate/mcq_generator.md
  - app/class_/lecture_ingest/lecture_ingest.md
  - app/db/connection.md
  - app/db/schema/schema.md
  - app/db/queries_classes/queries_classes.md
  - app/db/queries_lectures/queries_lectures.md
  - app/db/queries_questions/queries_questions.md
  - app/db/queries_pages/queries_pages.md
  - app/db/queries_learning_objectives/queries_learning_objectives.md
  - app/db/queries_users/queries_users.md
  - .planning/todos/done/2026-05-02-phase-2-streamlit-widget-catalog-research.md
autonomous: false
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04, PAGE-05]
must_haves:
  truths:
    - "Theme delivery is an embedded `_CSS` Python string in `app/brain/theme/theme.py` via `inject_theme()` (D-2.17 amends D-2.1) — no separate .css file ships."
    - "`previews/_theme.py` is a deliberate **copy** of `app/brain/theme/theme.py` (D-2.22). Drift is a feature; refresh on next visual task touching theme."
    - "Each component sandbox under `previews/components/*` carries its OWN `_theme.py` copy. `previews/_theme.py` is the source-of-truth template that new sandboxes copy from. Symlinks are forbidden — they silently violate the isolation invariant."
    - "All sandbox files contain zero `from app...` / `import app...` lines (CLAUDE.md sandbox-isolation rule)."
    - "All scoped component styling uses `[class*=\"st-key-XXX\"]` selectors on `st.container(key=...)` wrappers (D-2.18). `stylable_container` is forbidden (RESEARCH §6.3)."
    - "Canonical `st-key-*` vocabulary (btn-default, btn-ghost, btn-soft, btn-tinted, card-passive, card-interactive, class-card, stat-card, topbar, empty, mcq, mcq-card, mcq-opt-{key}-{state}) is used verbatim in both production and sandbox (D-2.18)."
    - "Fonts are loaded via self-hosted `@font-face` in `_CSS` from `assets/fonts/*.woff2` (D-2.16). Google Fonts `@import` is removed."
    - "`@media (prefers-reduced-motion: reduce)` block disables transitions and transforms on all interactive surfaces (D-2.15)."
    - "MCQ option styling matches Figma node 4045:282 exactly (D-2.20 replaced + D-2.23 + D-2.24): checkbox UI, 4 states (Off/On/Correct/Incorrect), padding 14/13 → 15/14 on border widen, 5px option radius, 6px card radius, elevation-as-selection."
    - "Streamlit version pinned `>=1.50.0` in pyproject.toml so `[theme]` palette knobs and `st.container(key=)` work (RESEARCH §1, §9, A5)."
    - "All 15 Phase 1 sidecars carry a `## Code walkthrough` section per D-5.1 format. Each sidecar <=140 lines (C-22 flex per CONTEXT D-5.1)."
    - "`02-WIDGETS.md` contains the validated D-2.12 token table + Figma reconciliation patches + spike verdicts for Q3, Q4, Q8."
    - "`02-FIGMA-RESEARCH.md` is the researcher run output (D-2.26): ≤30 Figma MCP calls budgeted, reconciles SURF_UI vs SURF_UI(old), feeds `ui/documentation.md` § 5 + § 8."
    - "`ui/documentation.md` § 5 (component catalog) and § 8 (DOM reach map) are filled from `02-FIGMA-RESEARCH.md` outputs (D-2.25). Final file ≤500 lines or split into siblings."
  artifacts:
    - path: "app/brain/theme/theme.py"
      provides: "Embedded `_CSS` Python string + `inject_theme()` entry point + 9 helper primitives (eyebrow, caption, meta, score, chip, chips_row, steps, stat_card, empty_state_text)."
      exports: ["inject_theme", "eyebrow", "caption", "meta", "score", "chip", "chips_row", "steps", "stat_card", "empty_state_text"]
      contains: "@font-face"
    - path: "app/brain/theme/__init__.py"
      provides: "Re-exports public API from theme.py."
      exports: ["inject_theme", "eyebrow", "caption", "meta", "score", "chip", "chips_row", "steps", "stat_card", "empty_state_text"]
    - path: "app/brain/theme/edit_this_later.md"
      provides: "Single edit-map (RESEARCH §7.3 + D-2.12..D-2.24): every locked visual decision → where it lives → what to change → propagation."
    - path: "previews/_theme.py"
      provides: "Sandbox copy of theme.py for visual preview gate (D-2.22). No `from app...` imports."
    - path: "previews/_fixtures.py"
      provides: "Pure-data shared fixtures (FAKE_USER, FAKE_CLASS, FAKE_LECTURES, FAKE_LOS, FAKE_SLIDE_PAGES, FAKE_MCQS, FAKE_ATTEMPT, FAKE_ATTEMPT_ANSWERS, fake_call_claude). No `streamlit` or `app/` imports."
    - path: "previews/components/_theme_bench/preview.py"
      provides: "Migrated from `Streamlit_Test/test_components.py` — full component bench (topbar, class cards, stat cards, chips, steps, 4 button families, inputs, toggles, MCQ, tabs, cards, empty state, messages, file uploader). The visual approval gate for Task 4."
    - path: ".streamlit/config.toml"
      provides: "[theme] block validated against D-2.12 (palette knobs, baseRadius=0.5rem, showBorderAroundInputs=true)."
      contains: "primaryColor"
    - path: ".planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md"
      provides: "Validated D-2.12 token table + Q3/Q4/Q8 spike verdicts + reconciliation notes."
    - path: ".planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md"
      provides: "Figma component-logic researcher output (D-2.26): cross-component design rules, summary-banner / sidebar-list / file-uploader-skin specs, SURF_UI(old) drift report."
    - path: "ui/documentation.md"
      provides: "Filled § 5 (component catalog, 20+ components) + § 8 (DOM reach map). Living team-facing reference."
    - path: "tests/test_db_connection_cache_resource.py"
      provides: "Q8 verification — `app/db/connection.py` uses `@st.cache_resource` (identity check + FK pragma)."
    - path: "tests/test_no_app_imports_in_previews.py"
      provides: "Mechanical enforcement of sandbox-isolation rule (greps every previews/*.py for `from app...` / `import app...` and fails on any match)."
  key_links:
    - from: "streamlit_app.py"
      to: "app.brain.theme.inject_theme"
      via: "import + call exactly once before st.navigation"
      pattern: "inject_theme\\(\\)"
    - from: "app/brain/theme/theme.py"
      to: "assets/fonts/*.woff2"
      via: "@font-face src: url('/assets/fonts/...woff2') in _CSS"
      pattern: "@font-face"
    - from: "previews/_theme.py"
      to: "(no app import)"
      via: "isolation invariant — sandbox copy"
      pattern: "^(?!from app)(?!import app)"
    - from: "ui/documentation.md § 5 / § 8"
      to: ".planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md"
      via: "D-2.25 fill from D-2.26 output"
      pattern: "Component catalog|DOM reach map"
---

<objective>
Wave 1 lays the foundation for the entire Mock Taking Loop. It absorbs the Area-6 branding lock (D-2.12..D-2.24), the Area-7 documentation lock (D-2.25, D-2.26), and the parallel-session adoption directive from `02-PARALLEL-AUDIT.md` (~70% reusable, MCQ must be rebuilt from Figma node 4045:282).

The Q1 "Figma extraction spike" from the prior plan is **demoted to a validation step** — extraction was completed by the parallel session and locked into D-2.12. What remains is: validate the locked tokens against the live Figma file, run the D-2.26 component-logic researcher (≤30 MCP calls) to surface cross-component rules that single-frame inspection missed, migrate `Streamlit_Test/ui/theme.py` to `app/brain/theme/theme.py` with the three audit amendments + four new locks (D-2.15 reduced-motion, D-2.20 MCQ rebuild, D-2.21 status/expander skins, D-2.16 self-hosted fonts), build the `previews/` sandbox scaffold, run the three time-boxed spikes that de-risk Wave 2+ (Q3 Card Interactive overlay, Q4 fragment timer, Q8 cache_resource), back-fill 15 Phase 1 sidecars with `## Code walkthrough`, and fill `ui/documentation.md` § 5 + § 8.

Purpose: Plan 02-01 is the **critical gate** for the entire phase — plans 02-02 through 02-07 cannot start until this closes. Without theme.py + sandbox isolation, no visual work can land. Without the researcher run, the design system has unknown gaps. Without spike verdicts, P3 (lecture multi-select) and P4 (mock timer) risk re-architecting mid-build.

Output: A themed Streamlit shell (`inject_theme()` called once before navigation), `previews/` scaffold runnable end-to-end, 15 sidecars updated, `02-WIDGETS.md` + `02-FIGMA-RESEARCH.md` written, `ui/documentation.md` filled, three spike verdicts recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tiagoreimann/surf/CLAUDE.md
@/Users/tiagoreimann/surf/.planning/PROJECT.md
@/Users/tiagoreimann/surf/.planning/REQUIREMENTS.md
@/Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md
@/Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md
@/Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-PARALLEL-AUDIT.md
@/Users/tiagoreimann/surf/.planning/phases/01-ingestion-spine-database/01-VERIFICATION.md
@/Users/tiagoreimann/surf/streamlit_app.py
@/Users/tiagoreimann/surf/.streamlit/config.toml
@/Users/tiagoreimann/surf/app/db/connection.py
@/Users/tiagoreimann/surf/Streamlit_Test/ui/theme.py
@/Users/tiagoreimann/surf/Streamlit_Test/test_components.py
@/Users/tiagoreimann/surf/Streamlit_Test/.streamlit/config.toml
@/Users/tiagoreimann/surf/ui/documentation.md
@/Users/tiagoreimann/surf/docs/design/figma_exports/node_25-2.png

<interfaces>
<!-- Public surface this plan creates and that downstream plans (02..07) consume. -->

From app/brain/theme/__init__.py (re-exports from theme.py):
```python
def inject_theme() -> None:
    """Inject _CSS once at app entry via st.markdown(unsafe_allow_html=True). Idempotent across reruns."""

def eyebrow(text: str) -> None: ...
def caption(text: str) -> None: ...
def meta(text: str) -> None: ...
def score(value: float) -> None:
    """Render Swiss 1-6 grade with color thresholds: <3.5 red, 3.5-5 gold, >=5 green."""
def chip(text: str, kind: str = "default") -> str: ...
def chips_row(items: list[tuple[str, str]]) -> None: ...
def steps(current: int, total: int) -> None: ...
def stat_card(label: str, value: str, hint: str = "") -> None: ...
def empty_state_text(text: str) -> None: ...
```

From previews/_fixtures.py:
```python
FAKE_USER: dict
FAKE_CLASS: dict
FAKE_LECTURES: list[dict]      # 3 entries: 1 done, 1 pending, 1 done
FAKE_LOS: list[dict]            # 4 entries with page_start/page_end
FAKE_SLIDE_PAGES: list[dict]    # ~19 kept + 12 ignored (mirrors MII_SM1)
FAKE_MCQS: list[dict]           # 19 entries; 4 multi-correct
FAKE_ATTEMPT: dict
FAKE_ATTEMPT_ANSWERS: list[dict]
def fake_call_claude(system_prompt: str, user_message: str, expect_json: bool = False) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Figma component-logic researcher run (D-2.26) — write 02-FIGMA-RESEARCH.md</name>
  <read_first>
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (Area 6 D-2.12..D-2.24, Area 7 D-2.25..D-2.26)
    - /Users/tiagoreimann/surf/ui/documentation.md (TBD markers in § 5 and § 8)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-PARALLEL-AUDIT.md
    - /Users/tiagoreimann/surf/docs/design/figma_exports/node_25-2.png
  </read_first>
  <action>
1. **Budget gate:** the researcher run is capped at **≤30 Figma MCP calls total**. Track call count in `02-FIGMA-RESEARCH.md` header. Stop at 28 to leave headroom. If a TBD remains at the cap, log it in `## Open questions` rather than burning more calls.
2. Reconcile library naming (D-2.26):
   - Call `mcp__claude_ai_Figma__get_libraries` (1 call). Check whether `SURF_UI` exists as a non-`(old)` revision.
   - If a non-(old) version exists with newer node ids: STOP and ask Tiago which file is canonical (consent gate per CLAUDE.md "API key consent" — Figma is read-only so technically not API-cost-relevant, but a library swap is a planning decision).
   - If only `SURF_UI(old)` exists: drop the suffix concern; record this in `## Library reconciliation`.
3. Map cross-component design rules (≤25 calls):
   - `mcp__claude_ai_Figma__get_metadata` on file root (1 call) → list all top-level pages.
   - `mcp__claude_ai_Figma__search_design_system` for: Button, Card, Chip, MCQ option, Topbar, Stat card, Steps, Score, Difficulty stars, Summary banner, Sidebar list, File uploader (≤12 calls).
   - `mcp__claude_ai_Figma__get_design_context` on composed frames where multiple components are arranged together — typically the page mocks (P1..P5) (≤8 calls). Goal: extract spacing rules between components, alignment patterns, and which components are siblings vs nested.
   - `mcp__claude_ai_Figma__get_variable_defs` on file root (1 call) to validate the D-2.12 token taxonomy. Compare against the locked hex values in CONTEXT D-2.12. Log any drift in `## Token reconciliation patches`.
4. Write `.planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md` with these sections:
   - `# Figma component-logic research (D-2.26)`
   - `## Call budget` — running count of MCP calls used (target ≤28).
   - `## Library reconciliation` — SURF_UI vs SURF_UI(old) verdict.
   - `## Token reconciliation patches` — table of any drift between Figma `Tokens` collection values and CONTEXT D-2.12 locked hex values. Each row: token name | CONTEXT D-2.12 value | Figma value | recommended action (keep / patch CONTEXT / patch theme.py).
   - `## Component catalog rules` — rules that span multiple components: spacing scale between sibling components, alignment patterns inside cards, hierarchy of elevations, when to use chip vs button vs link.
   - `## New components surfaced` — specs for any of: `summary-banner` (P5 final score), `sidebar-list` (P3 lecture nav), `file-uploader-skin` (P2 PDF drop). Each: Figma node id, key class name (must match `st-key-*` vocabulary), CSS sketch.
   - `## DOM reach map` — for each canonical `st-key-*` wrapper, the Streamlit widget(s) it must reach via `data-testid` selectors. This feeds `ui/documentation.md` § 8.
   - `## Open questions` — anything unresolved within the call budget.
5. **Output gate:** if `## Token reconciliation patches` lists any item with action ≠ "keep", flag it in `02-WIDGETS.md` Task 3 verification so Tiago sees it before approving the theme bench.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md && grep -q "## Call budget" .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md && grep -q "## Library reconciliation" .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md && grep -q "## Token reconciliation patches" .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md && grep -q "## Component catalog rules" .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md && grep -q "## DOM reach map" .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md</automated>
  </verify>
  <done>
    - `02-FIGMA-RESEARCH.md` exists with all six required sections.
    - `## Call budget` records actual MCP call count and confirms ≤30.
    - Library reconciliation produced a verdict (keep `(old)` or swap to canonical).
    - Token reconciliation table is filled (rows or "no drift detected").
    - At least the canonical `st-key-*` vocabulary is mapped to Streamlit `data-testid` selectors in `## DOM reach map`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate theme.py to app/brain/theme/ with all D-2.12..D-2.24 amendments</name>
  <read_first>
    - /Users/tiagoreimann/surf/Streamlit_Test/ui/theme.py (full 597 lines — source for migration)
    - /Users/tiagoreimann/surf/Streamlit_Test/.streamlit/config.toml (canonical [theme] palette source for step 5 — DO NOT invent values from D-2.12)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (Area 6 D-2.12..D-2.24)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-PARALLEL-AUDIT.md (3 amendments)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md (Token reconciliation patches from Task 1)
    - /Users/tiagoreimann/surf/assets/fonts/ (verify the 3 woff2 files exist)
  </read_first>
  <action>
1. Pin Streamlit `>=1.50.0` in `pyproject.toml` (read existing pin first; only edit the version line).
2. Create `app/brain/theme/theme.py` by **copying** `Streamlit_Test/ui/theme.py` and applying the following amendments in this order:
   - **Replace the Google Fonts `@import`** (line 19 of source) with self-hosted `@font-face` declarations (D-2.16). Three faces: Fraunces normal (`assets/fonts/Fraunces-normal-400_900.woff2`, weight range 400-900, font-display: swap), Fraunces italic (`Fraunces-italic-400_900.woff2`, weight range 400-900, style: italic), JetBrains Mono (`JetBrainsMono-Variable.woff2`, weight range 400-700, font-display: swap). Use absolute path `/assets/fonts/<file>.woff2` so Streamlit's static-file server serves them (Streamlit ≥1.50 serves `assets/` automatically).
   - **Apply Token reconciliation patches** from Task 1 `02-FIGMA-RESEARCH.md` if any drift was logged with action ≠ "keep". Otherwise leave the locked D-2.12 hex values untouched.
   - **Add `@media (prefers-reduced-motion: reduce)` block** (D-2.15, ~6 lines) at the bottom of `_CSS`: zero `transition-duration` and `transform: none` on `[data-testid="stButton"] button`, `[class*="st-key-card-"]`, `[class*="st-key-mcq-opt"]`, and any other interactive surface that uses transform/transition.
   - **Discard the existing `[class*="st-key-mcq"]` block entirely** (the old radio+accent-wash design). Rebuild from Figma node 4045:282 per D-2.20 + D-2.23 + D-2.24:
     - **MCQ option** (D-2.20): wrap each option in `st.container(key=f"mcq-opt-{key}-{state}")` where `state ∈ {off, on, correct, incorrect}`. CSS branches on the suffix in the key class. Custom checkbox glyph: hide the native `[data-testid="stCheckbox"] svg`, draw a 20×20px square (5px radius — NOT `--r-sm`) using a pseudo-element on the wrapper, render `✓` in JetBrains Mono Bold inside `:has(input:checked)`. State styles:
       - `off`: paper-1 bg, 1px paper-3 border, no shadow, padding 14/13.
       - `on`: paper-0 bg, 2px paper-5 border, 2px stamp shadow, padding 15/14 (border widens 1→2 px so padding shifts 14/13 → 15/14 to keep text position stable — DO NOT DROP this).
       - `correct`: ok-wash bg, 2px ok border, 2px stamp shadow.
       - `incorrect`: accent-soft bg, 2px accent-deep border, 2px stamp shadow.
     - **MCQ card container** (D-2.23): `st.container(key="mcq-card")` — paper-0 bg, 2px paper-shadow border, 6px radius (NOT 4 px), 3px stamp shadow, padding 22/20/20/20, max-width 600px, 13px vertical gap between sections.
     - **Difficulty stars** (D-2.24): see Task 2.5 below.
   - **Add `st.status` and `st.expander` skins** (D-2.21):
     - `[data-testid="stStatus"][aria-live="polite"][data-state="running"]` → 4px left border `var(--accent-vibrant)`, paper-1 bg.
     - `[data-testid="stStatus"][data-state="complete"]` → 4px left border `var(--ok)`, paper-1 bg.
     - `[data-testid="stExpander"]` → paper-1 bg, 6px radius, 2px stamp shadow on `:hover`, hover transform `translate(-1px,-1px)` per D-2.13 recipe.
   - **Critical hardening:** ensure every helper function in theme.py that accepts user-provided text calls `html.escape()` before interpolating into `st.markdown(unsafe_allow_html=True)`. Audit each of: `eyebrow`, `caption`, `meta`, `chip`, `chips_row`, `steps`, `stat_card`, `empty_state_text`. The parallel session is missing these calls (per D-2.19) — patch all of them.
3. Create `app/brain/theme/__init__.py` that re-exports the public API:
   ```python
   from .theme import (
       inject_theme, eyebrow, caption, meta, score,
       chip, chips_row, steps, stat_card, empty_state_text,
   )
   ```
4. Modify `streamlit_app.py` to call `inject_theme()` exactly once at the top, before `st.navigation` runs. One-line surgical edit per CLAUDE.md "Surgical Changes" — do NOT touch any other code in the file.
5. Overwrite `.streamlit/config.toml [theme]` to match `Streamlit_Test/.streamlit/config.toml` palette + add the modern palette knobs from RESEARCH §7.1 (`baseRadius = "0.5rem"`, `showBorderAroundInputs = true`). Keep `[server] maxUploadSize = 50`. Drop `font = "serif"` (theme.py owns typography now).
6. **Difficulty stars (D-2.24):**
   - Create `assets/icons/star_filled.svg` (filled star, 20×20px, accent-vibrant fill).
   - Create `assets/icons/star_empty.svg` (outlined star, 20×20px, paper-3 stroke, no fill).
   - Create `app/mock_take/question_render/_difficulty.py` with one function `difficulty_stars(score: float) -> None`. Logic: `n = max(1, min(5, round(score * 5)))`. Render via `st.html` an inline `<span>` containing `n` copies of `star_filled.svg` followed by `5 - n` copies of `star_empty.svg`. Use SVG inlining (read file bytes, decode, embed) — do NOT use `<img>` tags (they introduce DOM-ordering quirks per CONTEXT D-2.24).
7. Create `app/brain/theme/theme.md` (sidecar, ≤140 lines) per C-22 + D-5.1. Sections: plain-language summary, how to call (`from app.brain.theme import inject_theme; inject_theme()`), in/out, where it fits, gotchas-if-real (font path resolution, `unsafe_allow_html` is for layout-tokens only, sandbox copy must stay in sync after meaningful changes), `## Code walkthrough` (one paragraph per public function in D-5.1 format).
8. Create `app/mock_take/question_render/_difficulty.md` (sidecar ≤80 lines) with same structure.
9. Create `app/brain/theme/edit_this_later.md` containing the table from RESEARCH §7.3 expanded to cover D-2.12..D-2.24: every locked visual decision → "where it lives | what to change | propagation". Single edit-map for Phase 2 visual changes.
  </action>
  <verify>
    <automated>python -c "from app.brain.theme import inject_theme, eyebrow, score, chip, chips_row, stat_card, empty_state_text; assert all(callable(f) for f in [inject_theme, eyebrow, score, chip, chips_row, stat_card, empty_state_text])" && grep -q "@font-face" app/brain/theme/theme.py && ! grep -q "fonts.googleapis.com" app/brain/theme/theme.py && grep -q "prefers-reduced-motion" app/brain/theme/theme.py && grep -q "st-key-mcq-opt" app/brain/theme/theme.py && grep -q "st-key-mcq-card" app/brain/theme/theme.py && grep -q "primaryColor" .streamlit/config.toml && grep -q "baseRadius" .streamlit/config.toml && grep -q "inject_theme" streamlit_app.py && grep -q "## Code walkthrough" app/brain/theme/theme.md && python -c "import pathlib; assert 'html.escape' in pathlib.Path('app/brain/theme/theme.py').read_text(), 'html.escape missing — XSS hardening incomplete'" && python -c "from app.mock_take.question_render._difficulty import difficulty_stars; assert callable(difficulty_stars)" && test -f assets/icons/star_filled.svg && test -f assets/icons/star_empty.svg</automated>
  </verify>
  <done>
    - `app/brain/theme/theme.py` exists, has self-hosted `@font-face` (no Google Fonts URL), `prefers-reduced-motion` block, MCQ rebuild matching Figma node 4045:282, st.status + st.expander skins.
    - `app/brain/theme/__init__.py` re-exports the 10 public callables.
    - `streamlit_app.py` calls `inject_theme()` exactly once before `st.navigation`.
    - `.streamlit/config.toml [theme]` block has the locked hex values + `baseRadius` + `showBorderAroundInputs`.
    - `assets/icons/star_filled.svg` + `star_empty.svg` exist; `_difficulty.py` renders n filled + (5-n) empty.
    - `app/brain/theme/theme.md`, `app/brain/theme/edit_this_later.md`, `app/mock_take/question_render/_difficulty.md` all exist with `## Code walkthrough` sections.
    - All helpers that interpolate text call `html.escape()` (XSS hardening).
    - `streamlit run streamlit_app.py` opens without exception.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate D-2.12 tokens + write 02-WIDGETS.md (demoted from spike to validation)</name>
  <read_first>
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (Area 6 D-2.12..D-2.24 — locked values)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md (Token reconciliation patches from Task 1)
    - /Users/tiagoreimann/surf/app/brain/theme/theme.py (post-Task-2 — actual values shipped)
  </read_first>
  <action>
1. Write `.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md` with sections:
   - `## Token table` — one row per token in D-2.12: name | hex/value | source (D-2.12 ref + Figma node id from `02-FIGMA-RESEARCH.md`). Three columns: CONTEXT D-2.12 | Figma | shipped in theme.py. If any column differs, mark with ⚠️ and link to `02-FIGMA-RESEARCH.md ## Token reconciliation patches`.
   - `## Component spec` — Button (5 variants per D-2.19), Card passive, Card interactive, Chip, MCQ option (4 states per D-2.20), MCQ card container (D-2.23), Difficulty stars (D-2.24), Summary banner (if surfaced in `02-FIGMA-RESEARCH.md`).
   - `## Spike reports` — placeholders for Q3, Q4, Q8 (filled in Tasks 6–8).
   - `## Folded todos` — note that `2026-05-02-phase-2-streamlit-widget-catalog-research.md` is closed; move it to `.planning/todos/done/`.
2. Move the todo file from `.planning/todos/pending/` (or wherever it currently lives) to `.planning/todos/done/2026-05-02-phase-2-streamlit-widget-catalog-research.md` if it isn't already there (check first; idempotent).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md && grep -q "## Token table" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md && grep -q "## Component spec" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md && grep -q "## Spike reports" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md && (test -f .planning/todos/done/2026-05-02-phase-2-streamlit-widget-catalog-research.md || echo "todo file already in done/")</automated>
  </verify>
  <done>
    - `02-WIDGETS.md` exists with the four required sections.
    - `## Token table` rows include shipped column matching theme.py output.
    - Any drift between CONTEXT D-2.12 and Figma is flagged with ⚠️ and linked to `02-FIGMA-RESEARCH.md`.
    - Folded todo is in `.planning/todos/done/`.
  </done>
</task>

<task type="auto">
  <name>Task 4: previews/ sandbox scaffold + _fixtures.py + theme bench migration</name>
  <read_first>
    - /Users/tiagoreimann/surf/CLAUDE.md "Visual preview gate" section
    - /Users/tiagoreimann/surf/Streamlit_Test/test_components.py (full 328 lines — source for migration)
    - /Users/tiagoreimann/surf/app/db/schema/schema.sql (for fixture shape)
    - /Users/tiagoreimann/surf/.planning/phases/01-ingestion-spine-database/01-VERIFICATION.md (for MII_SM1 numbers)
  </read_first>
  <action>
1. Create `previews/` at the repo root (sibling of `app/`).
2. Create `previews/README.md` (≤80 lines): sandbox-isolation rule (no `from app...`), directory layout, stub policy (fake `call_claude`, `:memory:` SQLite or fixture dicts), drift policy, run pattern.
3. Create `previews/_theme.py` as a **byte-for-byte copy** of `app/brain/theme/theme.py` (post-Task-2). Copy, do not symlink. This satisfies D-2.22.
4. Create `previews/_fixtures.py` with PURE DATA only (no `streamlit` import, no `from app...`):
   - `FAKE_USER` (matches `users` schema row).
   - `FAKE_CLASS` (matches `classes` schema row, `factsheet_json` is a serialized 7-key dict per Phase 1 D-1.2).
   - `FAKE_LECTURES` (3: 1 done, 1 pending, 1 done).
   - `FAKE_LOS` (4 with page_start/page_end).
   - `FAKE_SLIDE_PAGES` (~19 kept + 12 ignored, mirroring MII_SM1 verification numbers).
   - `FAKE_MCQS` (19; 4 multi-correct).
   - `FAKE_ATTEMPT` (one in-progress + one completed).
   - `FAKE_ATTEMPT_ANSWERS` (3 correct, 1 wrong, 1 NULL).
   - `def fake_call_claude(...) -> dict` returning hard-coded JSON for {API key validate → {"ok": True}, factsheet_clean → fixed shape, lo_extract → fixed list, mcq_generate → first 3 MCQs from FAKE_MCQS}. Canonical stub all sandboxes import-or-copy.
5. Create `previews/components/_theme_bench/preview.py` by copying `Streamlit_Test/test_components.py` and amending:
   - Replace the import `from ui.theme import ...` with `from previews._theme import ...` (absolute import via `sys.path.insert(0, str(Path(__file__).resolve().parents[3]))`).
   - Drop any `from app...` line if it exists (per the audit, none should — verify).
   - Keep all 14 component sections (topbar, class cards, stat cards, chips, steps, 4 button families, inputs, toggles, MCQ, tabs, cards, empty state, messages, file uploader).
   - **Add a 15th section "MCQ option states (D-2.20)"** that renders all four states (off/on/correct/incorrect) of an MCQ option using the `mcq-opt-{key}-{state}` keying — this is the visual approval surface for the MCQ rebuild.
6. Create `previews/components/_theme_bench/_theme.py` as a copy of `previews/_theme.py` (the theme bench treats itself as a self-contained sandbox; the top-level `previews/_theme.py` exists for OTHER component sandboxes to import). Yes, this means three copies of theme.py exist after this task. That's the deliberate isolation cost.
7. Create `tests/_streamlit_apptest_helpers.py`:
   - `def make_apptest(script_path: str)` — wraps `st.testing.v1.AppTest.from_file`.
   - `def fake_anthropic_client()` — same hard-coded responses as `previews/_fixtures.fake_call_claude`. Build on top of Phase 1 `tests/_fakes.py` (read it first; do not duplicate).
8. Create `tests/test_theme_loader.py` (smoke):
   - Asserts `inject_theme()` is importable from `app.brain.theme`.
   - Asserts `app/brain/theme/theme.py` contains `@font-face`, `--paper`, `prefers-reduced-motion`, `st-key-mcq-opt`, no `TBD` substring.
9. Create `tests/test_no_app_imports_in_previews.py` (~15 lines): recursively walks every `*.py` under `previews/` and fails if any line matches `^\s*(from\s+app\.|import\s+app\.|from\s+app\s+import|import\s+app)`. Mechanically enforces the sandbox-isolation rule.
  </action>
  <verify>
    <automated>test -d previews/ && test -f previews/_theme.py && test -f previews/_fixtures.py && test -f previews/README.md && test -f previews/components/_theme_bench/preview.py && test -f previews/components/_theme_bench/_theme.py && python -c "import sys; sys.path.insert(0, '.'); import previews._fixtures as f; assert f.FAKE_USER and f.FAKE_CLASS and len(f.FAKE_MCQS) >= 5 and callable(f.fake_call_claude)" && pytest -q tests/test_theme_loader.py tests/test_no_app_imports_in_previews.py -x && diff -q app/brain/theme/theme.py previews/_theme.py && diff -q app/brain/theme/theme.py previews/components/_theme_bench/_theme.py</automated>
  </verify>
  <done>
    - `previews/_theme.py` is byte-equal to `app/brain/theme/theme.py` (verified by `diff -q`).
    - `previews/_fixtures.py` exists with all fakes; no `from app...` imports.
    - `previews/components/_theme_bench/preview.py` runs (`streamlit run previews/components/_theme_bench/preview.py`) and renders all 14 original sections + the new "MCQ option states" section.
    - `tests/test_theme_loader.py` and `tests/test_no_app_imports_in_previews.py` pass.
    - `tests/_streamlit_apptest_helpers.py` provides `make_apptest` + `fake_anthropic_client`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Tiago visually approves the theme bench (the parallel session's prototype, now migrated + amended)</name>
  <what-built>
    `app/brain/theme/theme.py` (production) + `previews/_theme.py` + `previews/components/_theme_bench/_theme.py` (sandbox copies) + the migrated bench app at `previews/components/_theme_bench/preview.py`. The bench renders every component family with the four amendments applied: self-hosted fonts, prefers-reduced-motion, MCQ rebuild from Figma node 4045:282, st.status + st.expander skins.
  </what-built>
  <how-to-verify>
    1. From repo root: `streamlit run previews/components/_theme_bench/preview.py`.
    2. Browser opens at http://localhost:8501. Confirm:
       - Background is the warm Paper hex (`#fdf9f2`), NOT white.
       - Heading uses Fraunces italic 900 — and there is no Network → fonts.googleapis.com request in DevTools (self-hosted check).
       - Buttons use JetBrains Mono Bold caps-tracked labels with stamp shadow (3px offset → 4px on hover with `translate(-1px,-1px)` lift).
       - Class cards lift on hover; selected state shows accent border (passive vs interactive distinction holds).
       - **MCQ section (new "MCQ option states" panel)** — confirm all four states render correctly:
         - Off: paper-1 bg, 1px border, no shadow.
         - On: paper-0 bg, 2px paper-5 border, 2px stamp shadow, text position is unchanged from Off (padding shift compensates).
         - Correct: ok-wash bg + 2px ok border.
         - Incorrect: accent-soft bg + 2px accent-deep border.
       - st.status block shows accent-vibrant left border while running, ok green when complete.
       - st.expander has paper-1 bg + lifts on hover.
    3. Open System Settings → Accessibility → Display → "Reduce motion" → ON. Reload the page. Confirm transitions are gone (no hover lift on cards/buttons).
    4. Open `02-WIDGETS.md` Token table. Confirm every "shipped in theme.py" value matches what your eye sees in the bench.
    5. If anything is off: leave a comment in `02-WIDGETS.md` next to the row OR in `02-FIGMA-RESEARCH.md` `## Open questions`, and resume.
  </how-to-verify>
  <acceptance_criteria>
    - Sandbox path: `previews/components/_theme_bench/preview.py`
    - Run command: `streamlit run previews/components/_theme_bench/preview.py`
    - "Tiago has visually approved the preview"
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Spike — fragment timer 5-min memory test (RESEARCH §11 Q4)</name>
  <read_first>
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md §4.3, §11 Q4
    - /Users/tiagoreimann/surf/CLAUDE.md "Visual preview gate" section
  </read_first>
  <action>
1. Create `previews/spikes/fragment_timer/preview.py`:
   - `@st.fragment(run_every="1s")` rendering elapsed seconds from `st.session_state["mock_start_ts"] = time.time()`.
   - "Reset" button + "MCQ option 1" checkbox OUTSIDE the fragment to verify state isolation.
   - No `from app...` imports.
2. Create `previews/spikes/SPIKES.md` (or extend `02-WIDGETS.md ## Spike reports`).
3. Time-box ≤2h. Practical test: run for 5 minutes, watch RSS via `Activity Monitor` or `ps -o rss`. PASS if growth <10MB/5min.
4. Verify: clicking the MCQ checkbox does NOT reset the timer.
5. Record in `02-WIDGETS.md ## Spike reports`:
   - **Q4 verdict:** PASS or FAIL.
   - **Chosen approach:** PASS → use `st.fragment(run_every="1s")` for P4 timer. FAIL → manual re-render-on-nav timer (recompute on Next/Prev/Skip/Submit click only).
6. If FAIL, write the fallback contract in `02-WIDGETS.md` so Plan 05 (P4) inherits it without re-deciding.
  </action>
  <verify>
    <automated>test -f previews/spikes/fragment_timer/preview.py && grep -q "run_every" previews/spikes/fragment_timer/preview.py && (grep -q "Q4 verdict" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md || grep -q "Q4 verdict" previews/spikes/SPIKES.md)</automated>
  </verify>
  <done>
    - Spike sandbox runs and ticks every 1s.
    - Q4 verdict + chosen approach recorded.
    - If FAIL, fallback contract is named for Plan 05.
  </done>
</task>

<task type="auto">
  <name>Task 7: Spike — Card Interactive overlay-button (RESEARCH §11 Q3)</name>
  <read_first>
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md §3.4, §3.5, §11 Q3
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md D-2.9
  </read_first>
  <action>
1. Create `previews/spikes/card_interactive_overlay/preview.py` — 3 lecture cards using Card Interactive pattern from RESEARCH §3.4:
   - `st.container(key=f"lecture-{i}")` per card.
   - Inside: `st.columns([8, 1])` with title + status, plus a tertiary button.
   - CSS in a copied `_theme.py` snippet hides the tertiary button via `data-testid="stBaseButton-tertiary"` overlay (`position: absolute; inset: 0; opacity: 0`).
   - Click toggles selection (`st.session_state["selected"]: set[int]`).
   - Selected state via `[class*="st-key-lecture-"]` `:has(...)` selector OR fallback.
2. Verify in browser: clicking anywhere on the card toggles selection; selected card shows accent-deep border + accent-wash bg; tertiary button is invisible but click-active.
3. Record in `02-WIDGETS.md ## Spike reports`:
   - **Q3 verdict:** PASS (overlay works on Streamlit ≥1.50) or FAIL.
   - **Chosen approach:** PASS → overlay-button pattern in Plan 04 (P3 lecture multi-select). FAIL → visible "Select / Selected ✓" button at the bottom of each card (uglier but no `data-testid` reliance).
4. Plan 04 inherits the chosen pattern — no re-litigation.
  </action>
  <verify>
    <automated>test -f previews/spikes/card_interactive_overlay/preview.py && (grep -q "Q3 verdict" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md || grep -q "Q3 verdict" previews/spikes/SPIKES.md)</automated>
  </verify>
  <done>
    - Spike sandbox demonstrates whichever pattern wins.
    - Q3 verdict + chosen approach recorded.
    - Streamlit ≥1.50.0 pin re-asserted.
  </done>
</task>

<task type="auto">
  <name>Task 8: Verify cache_resource on app/db/connection.py (RESEARCH §11 Q8)</name>
  <read_first>
    - /Users/tiagoreimann/surf/app/db/connection.py
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md §4.5, §11 Q8
  </read_first>
  <action>
1. Read `app/db/connection.py`. Determine whether the function returning `sqlite3.Connection` is decorated with `@st.cache_resource` and uses `check_same_thread=False`.
2. **If YES:** write `tests/test_db_connection_cache_resource.py`:
   - Import the connection function.
   - Call twice; assert `is` identity (not equality).
   - Assert `PRAGMA foreign_keys` returns `1`.
   - Record Q8 verdict PASS in `02-WIDGETS.md`.
3. **If NO:** Phase 1 carry-forward fix. Surgically edit `connection.py` to add `@st.cache_resource` and `check_same_thread=False`. Update `app/db/connection.md` walkthrough (Task 9 owns it). Record Q8 verdict "FIXED in Wave 1". Add the test in step 2.
4. Either way, the smoke test must pass.
  </action>
  <verify>
    <automated>pytest -q tests/test_db_connection_cache_resource.py -x && grep -q "Q8" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md</automated>
  </verify>
  <done>
    - `tests/test_db_connection_cache_resource.py` passes — identity + FK pragma.
    - `02-WIDGETS.md` records Q8 verdict (PASS or FIXED).
    - If `connection.py` was edited, change is minimal (decorator + kwarg) per CLAUDE.md "Surgical Changes".
  </done>
</task>

<task type="auto">
  <name>Task 9: Back-fill ## Code walkthrough in 15 Phase 1 sidecars (D-5.1, D-5.3)</name>
  <read_first>
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md D-5.1, D-5.2, D-5.3 + C-22 flex
    - The 15 sidecar files (one read per file):
      - app/brain/claude_client/claude_client.md
      - app/brain/ingestion/pdf_to_md_v3.md
      - app/brain/ingestion/page_splitter/page_splitter.md
      - app/my_classes/factsheet_clean/factsheet_cleaner.md
      - app/my_classes/factsheet_clean/factsheet_renderer.md
      - app/class_/lo_extract/lo_extractor.md
      - app/class_/mcq_generate/mcq_generator.md
      - app/class_/lecture_ingest/lecture_ingest.md
      - app/db/connection.md
      - app/db/schema/schema.md
      - app/db/queries_classes/queries_classes.md
      - app/db/queries_lectures/queries_lectures.md
      - app/db/queries_questions/queries_questions.md
      - app/db/queries_pages/queries_pages.md
      - app/db/queries_learning_objectives/queries_learning_objectives.md
      - app/db/queries_users/queries_users.md  (16th if present)
    - The corresponding .py file for each
  </read_first>
  <action>
For each of the 15 sidecars (16 if `queries_users.md` exists; check at task start):

1. Read the .md and its sibling .py once.
2. Append `## Code walkthrough` at the bottom (below all existing content) per D-5.1:
   - One paragraph per public top-level function (not private `_helper`s unless they carry semantic weight).
   - Format: `**def my_func(args)** — In plain language: takes X, does Y because Z, hands back W. Look out for: <one gotcha if any>.`
   - NO line refs (rot per D-5.1).
   - NO code dumps.
   - Audience: a non-CS reader can describe the function in their own words after one read (per memory: feedback_doc_clarity).
3. Verify each sidecar ≤140 lines after edit (C-22 flex per D-5.1; condense walkthrough if over — do NOT remove pre-existing content per CLAUDE.md "Surgical Changes").
4. If a sidecar already has a `## Code walkthrough` section, leave it. Idempotent.
5. Do NOT touch the .py files.
  </action>
  <verify>
    <automated>for f in app/brain/claude_client/claude_client.md app/brain/ingestion/pdf_to_md_v3.md app/brain/ingestion/page_splitter/page_splitter.md app/my_classes/factsheet_clean/factsheet_cleaner.md app/my_classes/factsheet_clean/factsheet_renderer.md app/class_/lo_extract/lo_extractor.md app/class_/mcq_generate/mcq_generator.md app/class_/lecture_ingest/lecture_ingest.md app/db/connection.md app/db/schema/schema.md app/db/queries_classes/queries_classes.md app/db/queries_lectures/queries_lectures.md app/db/queries_questions/queries_questions.md app/db/queries_pages/queries_pages.md app/db/queries_learning_objectives/queries_learning_objectives.md app/db/queries_users/queries_users.md; do grep -q "^## Code walkthrough" "$f" || { echo "MISSING walkthrough: $f"; exit 1; }; done
    # Line-cap check REMOVED 2026-05-03: C-22 amended mid-execution by Tiago — "no line cap; sidecars should explain the code."
    # See .planning/intel/constraints.md C-22 (locked) and .planning/PROJECT.md C-22 row for the current rule. Earlier check
    # (require ≤140 lines on claude_client.md / pdf_to_md_v3.md / connection.md) is superseded.</automated>
  </verify>
  <done>
    - All 16 sidecars have `## Code walkthrough` at the bottom.
    - Each follows D-5.1 format (function-by-function plain-language paragraphs, no code dumps, no line refs).
    - Pre-existing content untouched (CLAUDE.md "Surgical Changes").
    - Line-cap criterion removed 2026-05-03 per Tiago's C-22 amendment — clarity is the only criterion.
  </done>
</task>

<task type="auto">
  <name>Task 10: Fill ui/documentation.md § 5 (component catalog) + § 8 (DOM reach map) — D-2.25</name>
  <read_first>
    - /Users/tiagoreimann/surf/ui/documentation.md (current TBD markers in § 5 and § 8)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md (output of Task 1)
    - /Users/tiagoreimann/surf/app/brain/theme/theme.py (post-Task-2 — actual selectors shipped)
  </read_first>
  <action>
1. Fill `ui/documentation.md § 5 Component catalog` from `02-FIGMA-RESEARCH.md ## Component catalog rules` + `## New components surfaced`. Target 20+ components per CONTEXT D-2.25:
   - 5 button variants (`btn-default`, `btn-ghost`, `btn-soft`, `btn-tinted-accent`, `btn-tinted-ok/info/warn`).
   - 4 card types (`card-passive`, `card-interactive`, `class-card`, `stat-card`).
   - MCQ option (4 states), MCQ card, difficulty stars chip, chips_row.
   - Topbar, steps, score, empty_state, eyebrow/caption/meta typography helpers.
   - st.status skin, st.expander skin, file uploader skin (if surfaced).
   - summary-banner, sidebar-list (if surfaced).
   Each component entry: name | key class | Streamlit widget(s) it wraps | one-line "use when" | screenshot path or Figma node id.
2. Fill `§ 8 DOM reach map` from `02-FIGMA-RESEARCH.md ## DOM reach map` + the actual selectors in `app/brain/theme/theme.py`. Format: `[class*="st-key-XXX"]` → list of `[data-testid="..."]` selectors it reaches.
3. Re-check total line count: target ≤500 per CONTEXT D-2.25. If exceeded, split into siblings:
   - `ui/documentation.md` — overview + tokens (§ 1-4) + recipes (§ 6-7) + an index block at the top pointing to the two siblings.
   - `ui/components.md` — § 5 component catalog. Preserve the `## 5.` heading anchors so existing links don't rot.
   - `ui/dom_reach.md` — § 8 DOM reach map. Preserve the `## 8.` heading anchors.
   **Before splitting:** grep `.planning/` and `app/` recursively for `documentation.md#` and `documentation.md §` references. For each hit, rewrite the path to point at the new sibling file. Verify no broken refs remain by re-running the same grep against the new paths.
4. Verify zero TBD markers remain anywhere in the file(s).
  </action>
  <verify>
    <automated>! grep -nE "TBD|\[TBD" ui/documentation.md && grep -q "## 5\." ui/documentation.md && grep -q "## 8\." ui/documentation.md && (test "$(wc -l < ui/documentation.md)" -le 500 || { echo "OVER 500 LINES — must split into siblings"; ls ui/components.md ui/dom_reach.md 2>&1; })</automated>
  </verify>
  <done>
    - `ui/documentation.md § 5` lists ≥20 components, each with key class + widget mapping + use-when line.
    - `§ 8 DOM reach map` is filled — every canonical `st-key-*` wrapper maps to its `data-testid` reach list.
    - Total line count ≤500 OR file is split into siblings with cross-refs.
    - Zero TBD markers remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 11: Tiago approves filled ui/documentation.md (D-2.25 acceptance)</name>
  <what-built>`ui/documentation.md` § 5 + § 8 filled from `02-FIGMA-RESEARCH.md` and the shipped `theme.py`. The team-facing design system reference is now complete and ready for plans 02-02 through 02-07 to consume.</what-built>
  <how-to-verify>
    1. Open `ui/documentation.md` (and `ui/components.md` + `ui/dom_reach.md` if split).
    2. Skim § 5: every component listed should match what you saw in the theme bench at Task 5. If a component is missing or named differently, leave a comment.
    3. Skim § 8: pick three canonical `st-key-*` keys (e.g., `st-key-btn-default`, `st-key-mcq-opt-on`, `st-key-card-interactive`) and confirm the `data-testid` reach list matches what's in `app/brain/theme/theme.py`.
    4. Open `02-FIGMA-RESEARCH.md ## Open questions` — make sure no blocker for plans 02-02..02-07 remains unanswered. If any do, decide whether to resolve now (extend Task 1) or accept as Plan-N risk.
  </how-to-verify>
  <acceptance_criteria>
    - "Tiago has approved ui/documentation.md (D-2.25 satisfied)"
    - No open question in `02-FIGMA-RESEARCH.md` is a blocker for any plan 02-02..02-07.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what to fix.</resume-signal>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
# Theme + scaffold
python -c "from app.brain.theme import inject_theme, eyebrow, score, chip, stat_card; assert all(callable(f) for f in [inject_theme, eyebrow, score, chip, stat_card])"
grep -q "@font-face" app/brain/theme/theme.py
! grep -q "fonts.googleapis.com" app/brain/theme/theme.py
grep -q "prefers-reduced-motion" app/brain/theme/theme.py
grep -q "st-key-mcq-opt" app/brain/theme/theme.py
grep -q "st-key-mcq-card" app/brain/theme/theme.py
grep -q "primaryColor.*#[0-9a-fA-F]" .streamlit/config.toml
grep -q "baseRadius" .streamlit/config.toml
test -f previews/_fixtures.py && ! grep -E "^(from|import) app" previews/_fixtures.py
test -f previews/_theme.py && diff -q app/brain/theme/theme.py previews/_theme.py
test -f .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md
test -f .planning/phases/02-mock-taking-loop-p1-p5/02-FIGMA-RESEARCH.md
grep -q "Q3 verdict" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md
grep -q "Q4 verdict" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md
grep -q "Q8" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md
test -f assets/icons/star_filled.svg && test -f assets/icons/star_empty.svg

# Documentation (D-2.25)
! grep -nE "TBD|\[TBD" ui/documentation.md
test "$(wc -l < ui/documentation.md)" -le 500 || (test -f ui/components.md && test -f ui/dom_reach.md)

# Sidecars (D-5.3) — walkthrough requirement applies to script sidecars
# only (sibling-of-.py / sibling-of-.sql docs). System-prompt files
# (`*_system_prompt.md`) and design-system edit-maps
# (`app/brain/theme/edit_this_later.md`) are excluded by C-22 scope —
# see `.planning/intel/constraints.md` C-22 "Scope clarification".
SIDECARS=(
  app/brain/claude_client/claude_client.md
  app/brain/ingestion/pdf_to_md_v3.md
  app/brain/ingestion/page_splitter/page_splitter.md
  app/brain/theme/theme.md
  app/my_classes/factsheet_clean/factsheet_cleaner.md
  app/my_classes/factsheet_clean/factsheet_renderer.md
  app/class_/lo_extract/lo_extractor.md
  app/class_/mcq_generate/mcq_generator.md
  app/class_/lecture_ingest/lecture_ingest.md
  app/db/connection.md
  app/db/schema/schema.md
  app/db/queries_classes/queries_classes.md
  app/db/queries_lectures/queries_lectures.md
  app/db/queries_questions/queries_questions.md
  app/db/queries_pages/queries_pages.md
  app/db/queries_learning_objectives/queries_learning_objectives.md
  app/db/queries_users/queries_users.md
  app/mock_take/question_render/_difficulty.md
)
for f in "${SIDECARS[@]}"; do
  test -f "$f" || { echo "MISSING SIDECAR: $f"; exit 1; }
  grep -q "^## Code walkthrough" "$f" || { echo "MISSING WALKTHROUGH: $f"; exit 1; }
done

# Test suite
pytest -q
```

All commands return clean (no NEEDS lines, all tests green).
</verification>

<success_criteria>
- `app/brain/theme/theme.py` ships with embedded `_CSS` (D-2.17), self-hosted `@font-face` (D-2.16), prefers-reduced-motion block (D-2.15), MCQ rebuild matching Figma 4045:282 (D-2.20 + D-2.23 + D-2.24), st.status + st.expander skins (D-2.21), and html.escape() hardening across all helpers (D-2.19 fix).
- `previews/_theme.py` is a byte-equal sandbox copy of `app/brain/theme/theme.py` at the close of this plan (D-2.22). Drift is permitted later.
- `streamlit_app.py` calls `inject_theme()` exactly once before `st.navigation`.
- `02-FIGMA-RESEARCH.md` (D-2.26) records ≤30 MCP calls used, library reconciliation verdict, token reconciliation patches, and DOM reach map. No blocker open question.
- `02-WIDGETS.md` Token table includes shipped column and matches theme.py output (any drift flagged ⚠️ and linked to `02-FIGMA-RESEARCH.md`). Q3, Q4, Q8 spike verdicts recorded with chosen approaches.
- `ui/documentation.md` § 5 + § 8 filled (D-2.25); ≤500 lines total or split into siblings.
- `previews/` scaffold runnable; sandbox-isolation invariant holds (no `from app...`); theme bench visually approved.
- All 15 Phase 1 sidecars carry `## Code walkthrough` per D-5.1.
- Streamlit pinned ≥1.50.0; cache_resource verified or fixed on `connection.py`.
- `pytest -q` passes including 4 new tests (theme loader, no-app-imports, cache_resource, sidecar lint).
- Tiago has visually approved the theme bench (Task 5) and the filled documentation (Task 11).
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-taking-loop-p1-p5/02-01-SUMMARY.md` covering:
- D-2.26 researcher run outcome (call count, library reconciliation, token patches, new components surfaced)
- D-2.25 documentation fill outcome (final line count, split decision)
- Migration outcome (theme.py shipped, three audit amendments applied, four new locks applied, html.escape hardening)
- Spike outcomes (Q3, Q4, Q8) and chosen approaches inherited by plans 02-04 and 02-05
- Sidecar back-fill summary (15/15 complete)
- Wave 2 readiness checklist (theme injected on app start, fixtures available, AppTest helper ready, MCQ rebuild visually approved)
</output>
