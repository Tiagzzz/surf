---
phase: 02-mock-taking-loop-p1-p5
plan: 07
type: execute
wave: 6
depends_on: [02-01, 02-02, 02-03, 02-04a, 02-04b, 02-05, 02-06]
files_modified:
  - tests/test_e2e_happy_path.py
  - tests/test_smoke.py
  - app/brain/claude_client/claude_client.md
  - app/brain/ingestion/pdf_to_md_v3.md
  - app/brain/ingestion/page_splitter/page_splitter.md
  - app/my_classes/factsheet_clean/factsheet_cleaner.md
  - app/my_classes/factsheet_clean/factsheet_renderer.md
  - app/class_/lo_extract/lo_extractor.md
  - app/class_/mcq_generate/mcq_generator.md
  - app/class_/lecture_ingest/lecture_ingest.md
  - app/db/connection.md
  - app/db/schema/schema.md
  - app/db/queries_classes/queries_classes.md
  - app/db/queries_lectures/queries_lectures.md
  - app/db/queries_questions/queries_questions.md
  - app/db/queries_pages/queries_pages.md
  - app/db/queries_learning_objectives/queries_learning_objectives.md
  - app/db/queries_users/queries_users.md
  - app/signup/signup_flow/signup_flow.md
  - app/signup/api_key_validate/api_key_validate.md
  - app/my_classes/add_class/add_class.md
  - app/my_classes/class_card_render/class_card_render.md
  - app/class_/lecture_upload/lecture_upload.md
  - app/class_/build_mock/build_mock.md
  - app/class_/study_next/study_next.md
  - app/db/queries_attempts/queries_attempts.md
  - app/mock_take/question_render/question_render.md
  - app/mock_take/answer_capture/answer_capture.md
  - app/mock_take/attempt_save/attempt_save.md
  - app/mock_take/timer_render/timer_render.md
  - app/mock_review/question_render/question_render.md
  - app/mock_review/summary_banner/summary_banner.md
  - app/brain/grading_formula/grading_formula.md
  - app/brain/theme/theme.md
  - app/brain/theme/edit_this_later.md
  - .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md
autonomous: false
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04, PAGE-05, PIPE-02, MECH-01, MECH-02, MECH-03, GRADE-04]
must_haves:
  truths:
    - "An end-to-end happy-path AppTest exists that walks P1 → P2 → P3 → P4 → P5 against an in-memory SQLite + stubbed claude_client (MECH-01)"
    - "tests/test_smoke.py is extended (or augmented) to import every Phase 2 module without side effects so a clean clone passes pytest -q in <30s"
    - "All ~30 Phase 2 sidecars (the 15 Phase 1 back-fill from Plan 01 + the new Phase 2 ones from Plans 02-06) carry a ## Code walkthrough section, are <=140 lines (C-22 flex), and were touched in their owning plan (D-5.2 wave-closure cadence)"
    - "All locked decisions in 02-CONTEXT.md (D-2.1..D-2.11, D-3.1..D-3.5, D-4.1..D-4.3, D-5.1..D-5.3) are listed in 02-WIDGETS.md `## Decision coverage` table with the file/line where each lives"
    - "A final preview-gate sweep walks every approved sandbox under previews/ once more in production-like sequence and Tiago re-confirms the visual aesthetic across pages is coherent"
    - "Sandbox-isolation invariant holds repo-wide: zero `from app...` or `import app...` lines exist anywhere under previews/ (CLAUDE.md visual preview gate)"
  artifacts:
    - path: "tests/test_e2e_happy_path.py"
      provides: "Streamlit AppTest walking P1→P2→P3→P4→P5 against :memory: SQLite + stubbed claude_client"
      contains: "AppTest"
    - path: ".planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md"
      provides: "Final decision coverage table + spike outcomes + Figma extraction (cumulative — extended in this plan, not rewritten)"
      contains: "Decision coverage"
  key_links:
    - from: "tests/test_e2e_happy_path.py"
      to: "tests/_streamlit_apptest_helpers.py"
      via: "make_apptest + fake_anthropic_client"
      pattern: "make_apptest"
---

# Plan 02-07 — Phase 2 Closeout: E2E Happy Path + Sidecar Audit + Visual Sweep

## Objective

Plan 07 closes Phase 2: writes a single end-to-end AppTest covering P1→P5, audits every sidecar from Plans 01–06 against the C-22 + D-5.1 contract, records decision coverage in `02-WIDGETS.md`, and runs a final visual sweep (Tiago walks all approved sandboxes in sequence to confirm cross-page coherence).

Purpose: catches integration drift between waves, enforces the wave-closure ritual D-5.2 once-more for the Phase as a whole, and produces the artifact `gsd-verify-work` will read in the next session to certify the phase.

Output: one new test, ~15 sidecar touch-ups (delta-only), an extended `02-WIDGETS.md`, and the visual sweep checkpoint.

## Execution context

- Workflow: `~/.claude/get-shit-done/workflows/execute-plan.md`
- Summary template: `~/.claude/get-shit-done/templates/summary.md`

## Context

- /Users/tiagoreimann/surf/CLAUDE.md
- /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (every D-X.X locked decision)
- /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md (§8 validation; §11 open questions for the audit)
- /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md (Plan 01-06 cumulative state)
- /Users/tiagoreimann/surf/tests/test_smoke.py (Phase 1 baseline)
- /Users/tiagoreimann/surf/tests/_streamlit_apptest_helpers.py (Plan 01)
- /Users/tiagoreimann/surf/previews/_fixtures.py (Plan 01)

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: End-to-end AppTest happy path P1→P5</name>
  <read_first>
    - /Users/tiagoreimann/surf/tests/test_smoke.py
    - /Users/tiagoreimann/surf/tests/_streamlit_apptest_helpers.py
    - /Users/tiagoreimann/surf/streamlit_app.py
    - /Users/tiagoreimann/surf/views/signup.py
    - /Users/tiagoreimann/surf/views/my_classes.py
    - /Users/tiagoreimann/surf/views/class_view.py
    - /Users/tiagoreimann/surf/views/take_mock_exam.py
    - /Users/tiagoreimann/surf/views/review_mock_exam.py
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md §8.2 test map for MECH-01
  </read_first>
  <behavior>
    Behavior under test (RED first):
    - test_e2e_happy_path.test_signup_to_review_completes — Single AppTest scenario:
      1. Boot app with empty :memory: SQLite + stubbed `call_claude` (always returns valid for API-key validation; returns canned factsheet JSON for cleaner; returns canned LO + MCQ for ingest).
      2. P1: fill username + API key → submit → assert switched to P2.
      3. P2: upload a fake PDF → review screen renders → click Save → assert classes row count == 1 and switched to P3.
      4. P3: upload a fake lecture PDF → ingest stub yields 3 LOs + 6 MCQs → select all 1 lecture → Generate Mock → assert attempt with 5 questions created and switched to P4.
      5. P4: answer 3 correctly, skip 1, answer 1 wrong → Submit → assert complete_attempt set finished_at + correct_count=3 → switched to P5.
      6. P5: assert summary banner shows "Final note: 4.0" (5×3/5+1=4.0), 4 result cards rendered with correct status badges, 1 with Skipped badge.
    - test_smoke.test_phase2_module_imports — Imports every Phase 2 module to confirm no import-time side effects (Streamlit imports must be lazy enough to allow `python -c "import app.signup..."` without booting Streamlit).
  </behavior>
  <action>
    Action (GREEN):

    1. Extend `tests/_streamlit_apptest_helpers.py` if needed: a single `e2e_session()` context manager that wires the in-memory SQLite + stubbed `call_claude`. Build on top of Phase 1's `tests/_fakes.py` and Plan 01's helper rather than duplicating.
    2. Write `tests/test_e2e_happy_path.py` per the behavior list. Use `st.testing.v1.AppTest.from_file("streamlit_app.py")` and drive it via the AppTest API (`at.text_input["..."].set_value(...)`, `at.button["..."].click()`, `at.run()`). Reference: RESEARCH §8.4.
    3. Extend `tests/test_smoke.py` with an import-only smoke that walks every new Phase 2 module:
       ```
       importlib.import_module("app.signup.signup_flow.signup_flow")
       importlib.import_module("app.my_classes.add_class.add_class")
       ... etc
       ```
       Wrap each in `pytest.mark.skip(reason='streamlit missing')` only when the dependency is genuinely optional (matches Phase 1 pattern).
    4. Run `pytest -q` and assert wall-time <30s on a fresh checkout (RESEARCH §8.3 sampling rate).
  </action>
  <verify>
    <automated>pytest -q tests/test_e2e_happy_path.py tests/test_smoke.py -x &amp;&amp; pytest -q &amp;&amp; bash -c 'time pytest -q | tail -5'</automated>
  </verify>
  <done>
    - E2E test passes; covers all 5 pages + the Swiss formula + multi-correct grading + skipped semantics.
    - Full pytest -q is green and <30s.
    - No skipped tests in normal CI path (only the Phase-1-style `pytest.mark.skip` for missing optional deps).
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidecar audit + decision coverage table</name>
  <read_first>
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (every D-X.X)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md (Plans 01-06 cumulative)
    - All 30+ sidecar .md files (single-pass each — extract names + walkthrough presence)
  </read_first>
  <action>
    1. Programmatic audit:
       ```
       for f in $(find app -name "*.md" -type f); do
         pages=$(wc -l < "$f")
         wc=$(grep -c "^## Code walkthrough" "$f")
         [ "$pages" -le 140 ] || echo "OVER 140: $f ($pages lines)"
         [ "$wc" -ge 1 ] || echo "MISSING WALKTHROUGH: $f"
       done
       ```
       Capture the offenders. For each:
       - Over 140 lines → condense the walkthrough section (do NOT touch pre-existing content per CLAUDE.md Surgical Changes; only the sections this phase added).
       - Missing walkthrough → append the section per D-5.1 format.
    2. Append (extend, do not rewrite) `02-WIDGETS.md` with a `## Decision coverage` section:

       Table columns: `Decision ID | Description (1 line) | Lives in (file or section)`.

       Rows: every D-2.1..D-2.11, D-3.1..D-3.5, D-4.1..D-4.3, D-5.1..D-5.3 from CONTEXT. The `Lives in` column points to the .py / .md / .sql / .css file where the decision is now realized.

       Example row: `D-3.4 | Multi-correct grading all-or-nothing | app/brain/grading_formula/grading_formula.py + app/mock_take/answer_capture/answer_capture.py:capture_answer`.
    3. Append a `## Spike outcomes (final)` table to `02-WIDGETS.md` summarizing the outcomes of Q1, Q2, Q3, Q4, Q8 from RESEARCH §11. (Plans 01 already filled placeholders; this task confirms.)
    4. Run a final repo-wide invariant check:
       ```
       find previews -name "*.py" | xargs grep -l "^from app\\|^import app" || echo OK
       ```
       Expect "OK" — no `from app` imports under previews/.
  </action>
  <verify>
    <automated>find app -name "*.md" -type f | xargs -I{} sh -c 'lines=$(wc -l < "{}"); walk=$(grep -c "^## Code walkthrough" "{}"); test "$lines" -le 140 &amp;&amp; test "$walk" -ge 1 || echo "FAIL: {}"' | grep -v "^$" | (! grep -q FAIL) &amp;&amp; bash -c 'set +e; out=$(find previews -name "*.py" -print0 | xargs -0 grep -l "^from app\|^import app"); test -z "$out"' &amp;&amp; grep -q "## Decision coverage" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md &amp;&amp; grep -E "D-2.1|D-3.4|D-4.3|D-5.3" .planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md</automated>
  </verify>
  <done>
    - Every sidecar file under app/ is ≤140 lines and contains a walkthrough section.
    - Decision coverage table in 02-WIDGETS.md lists every D-X.X with its file location.
    - No `from app` imports anywhere under previews/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final cross-page visual sweep</name>
  <what-built>
    - A complete Phase 2 demo path: P1 → P2 → P3 → P4 → P5.
    - Decision coverage table + sidecar audit recorded.
  </what-built>
  <how-to-verify>
    Walk through the previously-approved sandboxes in production order. For each, confirm the **aesthetic feels coherent across pages** — same Paper palette, same monospace button labels, same card rhythm, same spacing.

    1. `streamlit run previews/components/theme_probe/preview.py` — re-confirm theme tokens.
    2. `streamlit run previews/pages/p1_signup/preview.py` — confirm sign-up form.
    3. `streamlit run previews/pages/p2_my_classes/preview.py` — confirm carousel + Add Class.
    4. `streamlit run previews/pages/p3_class_hub/preview.py` — confirm sidebar + lecture multi-select + Study Next.
    5. `streamlit run previews/pages/p4_take_mock/preview.py` — confirm timer + MCQ card + nav row.
    6. `streamlit run previews/pages/p5_review_mock/preview.py` — confirm summary banner + result cards + rationales.

    Tiago confirms:
    - The Paper palette renders consistently across all pages.
    - Monospace button labels are present and consistent.
    - Card rounding/shadow/hover behavior matches across page-card types.
    - Status palette (OK / Warn / Info) renders correctly on the P5 result cards.
    - Spacing rhythm (8-pt scale, generous card padding) reads as one design system, not five.

    Optionally: `streamlit run streamlit_app.py` from a clean state. CAUTION: this is the production path with the real ~/.surf/user.sqlite and triggers Anthropic on factsheet upload + lecture ingestion. Per CLAUDE.md API-key-consent memory, ASK before running anything past P1 against the real API.
  </how-to-verify>
  <acceptance_criteria>
    - All 6 sandbox runs succeed without exception.
    - "Tiago has visually approved the preview" (one statement covering the cross-page sweep — implicit in approving each individual page-sandbox preview gate during prior plans, restated here for the closeout).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what to fix.</resume-signal>
</task>

## Verification

- `pytest -q` green and <30s.
- `find app -name "*.md" -type f | xargs grep -L "^## Code walkthrough"` returns empty.
- `find previews -name "*.py" | xargs grep -l "^from app\\|^import app"` returns empty.
- `02-WIDGETS.md` contains the `## Decision coverage` and `## Spike outcomes (final)` sections.

## Success criteria

- All 10 Phase 2 requirements (PAGE-01..05, PIPE-02, MECH-01, MECH-02, MECH-03, GRADE-04) are exercised by the end-to-end test.
- Every locked decision in 02-CONTEXT.md is recorded in the `## Decision coverage` table.
- Every sidecar conforms to C-22 (≤140 with flex) + D-5.1 walkthrough format.
- Sandbox-isolation invariant holds repo-wide.
- Tiago has confirmed cross-page visual coherence.
- The phase is ready for `/gsd-verify-work`.

## Output

Create `.planning/phases/02-mock-taking-loop-p1-p5/02-07-SUMMARY.md` covering:
- E2E test outcome (PASS + wall-time).
- Sidecar audit summary (files touched + counts).
- Decision coverage table reference.
- Final visual sweep approval.
- Hand-off note to `/gsd-verify-work` (which artifacts to read first).
