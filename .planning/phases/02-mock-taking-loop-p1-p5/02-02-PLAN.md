---
phase: 02-mock-taking-loop-p1-p5
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - streamlit_app.py
  - views/signup.py
  - app/signup/__init__.py
  - app/signup/signup_flow/__init__.py
  - app/signup/signup_flow/signup_flow.py
  - app/signup/signup_flow/signup_flow.md
  - app/signup/api_key_validate/__init__.py
  - app/signup/api_key_validate/api_key_validate.py
  - app/signup/api_key_validate/api_key_validate.md
  - app/db/queries_users/queries_users.py
  - app/db/queries_users/queries_users.md
  - app/brain/theme/surf_theme.css
  - tests/test_signup.py
  - tests/test_api_key_validate.py
  - tests/test_auth_router.py
  - previews/components/signup_form/preview.py
  - previews/components/signup_form/signup_form.py
  - previews/components/signup_form/surf_theme.css
  - previews/pages/p1_signup/preview.py
  - previews/pages/p1_signup/signup_flow.py
  - previews/pages/p1_signup/api_key_validate.py
  - previews/pages/p1_signup/surf_theme.css
autonomous: false
requirements: [PAGE-01, PIPE-02, GRADE-04]
must_haves:
  truths:
    - "First-launch routes user to P1 Sign Up; subsequent launches skip P1 because ~/.surf/user.sqlite exists (PAGE-01, ROADMAP SC1, D-36)"
    - "Username + Anthropic API key are validated against Anthropic via a 1-token call_claude before save (PIPE-02, RESEARCH §2 st.spinner + claude_client)"
    - "Invalid API key surfaces an inline st.error and does NOT write to ~/.surf/user.sqlite"
    - "Valid sign-up writes a single row to users(id, username, anthropic_api_key, created_at) and switches to P2 via st.switch_page"
    - "P1 uses st.form + st.form_submit_button for batch submit (RESEARCH §4.1) — no rerun on per-keystroke"
    - "All Claude calls in app/ go through app.brain.claude_client.call_claude; sandbox uses a stubbed call_claude returning {'ok': True} or raising for invalid key"
    - "All scoped styling uses st.container(key=...) + .st-key-<key>; stylable_container is forbidden (RESEARCH §6.3)"
    - "The P1 sandbox lives at previews/pages/p1_signup/ and contains no `from app...` imports (CLAUDE.md visual preview gate)"
    - "All sidecar .md files <=140 lines and include ## Code walkthrough section (D-5.1)"
  artifacts:
    - path: "app/signup/signup_flow/signup_flow.py"
      provides: "render_signup_page() — Streamlit form rendering + submit handling"
      exports: ["render_signup_page", "is_authenticated"]
    - path: "app/signup/api_key_validate/api_key_validate.py"
      provides: "validate_anthropic_key(key: str) -> bool — 1-token claude_client ping"
      exports: ["validate_anthropic_key"]
    - path: "app/db/queries_users/queries_users.py"
      provides: "save_user, get_user_or_none — stdlib sqlite3 with ? placeholders"
      exports: ["save_user", "get_user_or_none"]
    - path: "views/signup.py"
      provides: "Thin Streamlit page wrapper calling app/signup/signup_flow.render_signup_page"
      contains: "render_signup_page"
    - path: "previews/pages/p1_signup/preview.py"
      provides: "P1 page sandbox with stubbed claude_client + :memory: SQLite"
    - path: "tests/test_signup.py"
      provides: "Unit + AppTest for P1 happy + invalid-key paths"
  key_links:
    - from: "views/signup.py"
      to: "app/signup/signup_flow.render_signup_page"
      via: "import + call"
      pattern: "render_signup_page\\(\\)"
    - from: "app/signup/signup_flow/signup_flow.py"
      to: "app/signup/api_key_validate.validate_anthropic_key"
      via: "import + call before save_user"
      pattern: "validate_anthropic_key"
    - from: "app/signup/api_key_validate/api_key_validate.py"
      to: "app/brain/claude_client.call_claude"
      via: "1-token messages.create"
      pattern: "call_claude"
    - from: "app/signup/signup_flow/signup_flow.py"
      to: "app/db/queries_users.save_user"
      via: "after successful validation"
      pattern: "save_user"
    - from: "streamlit_app.py"
      to: "app/signup/signup_flow.is_authenticated"
      via: "router gate"
      pattern: "is_authenticated"
---

<objective>
Plan 02 ships P1 Sign Up: a single-form page that takes username + Anthropic API key, validates the key against Anthropic via a 1-token Claude call, and on success writes one row to `users` and routes to P2. Also ships the production `is_authenticated()` gate (replacing today's `Path.home() / ".surf" / "user.sqlite"` heuristic in `streamlit_app.py`) so subsequent launches skip P1.

Purpose: PAGE-01 + PIPE-02 + the entry point of GRADE-04 user interactions. Without P1, every demo path requires a manual SQLite file.

Output: A working sign-up page, three new pipelines (`signup_flow`, `api_key_validate`, `queries_users`), the canonical sandbox at `previews/pages/p1_signup/`, and unit + AppTest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tiagoreimann/surf/CLAUDE.md
@/Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md
@/Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md
@/Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md
@/Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-01-SUMMARY.md
@/Users/tiagoreimann/surf/streamlit_app.py
@/Users/tiagoreimann/surf/views/signup.py
@/Users/tiagoreimann/surf/app/brain/claude_client/claude_client.py
@/Users/tiagoreimann/surf/app/brain/claude_client/claude_client.md
@/Users/tiagoreimann/surf/app/db/connection.py
@/Users/tiagoreimann/surf/app/db/schema/schema.sql
@/Users/tiagoreimann/surf/tests/_fakes.py
@/Users/tiagoreimann/surf/previews/_fixtures.py

<interfaces>
<!-- Public surface this plan creates that downstream plans (03-07) consume. -->

From app/signup/signup_flow/signup_flow.py:
```python
def render_signup_page() -> None:
    """Render P1 Sign Up form. On valid submit, writes user row + switch_page."""

def is_authenticated() -> bool:
    """True iff one users row exists in ~/.surf/user.sqlite. Replaces the file-existence heuristic."""
```

From app/signup/api_key_validate/api_key_validate.py:
```python
def validate_anthropic_key(key: str) -> bool:
    """Return True iff the key is accepted by Anthropic for a 1-token messages.create.
    Catches anthropic.AuthenticationError → False; lets other errors propagate."""
```

From app/db/queries_users/queries_users.py:
```python
def save_user(username: str, anthropic_api_key: str) -> int:
    """INSERT one row into users. Returns the new id. Raises if username already exists."""

def get_user_or_none() -> dict | None:
    """SELECT * FROM users LIMIT 1. Returns dict or None. Single-user app."""
```

Schema (already locked Phase 1):
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL UNIQUE,
    anthropic_api_key TEXT NOT NULL,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queries_users + api_key_validate + signup_flow (TDD)</name>
  <read_first>
    - /Users/tiagoreimann/surf/app/db/connection.py
    - /Users/tiagoreimann/surf/app/db/schema/schema.sql (users table — already exists)
    - /Users/tiagoreimann/surf/app/db/queries_classes/queries_classes.py (pattern reference)
    - /Users/tiagoreimann/surf/app/brain/claude_client/claude_client.py
    - /Users/tiagoreimann/surf/app/brain/claude_client/claude_client.md
    - /Users/tiagoreimann/surf/tests/_fakes.py
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md §4.1, §10
  </read_first>
  <behavior>
    - test_queries_users.test_save_user_then_get_returns_row — given empty :memory: DB, `save_user("tiago", "sk-ant-x")` returns int>0 and `get_user_or_none()` returns `{"username": "tiago", ...}`.
    - test_queries_users.test_save_user_unique — second `save_user("tiago", ...)` raises `sqlite3.IntegrityError` on UNIQUE constraint.
    - test_queries_users.test_get_user_or_none_empty — fresh DB returns None.
    - test_api_key_validate.test_valid_key_returns_true — fake `call_claude` returns success → `validate_anthropic_key("sk-ant-x")` is True.
    - test_api_key_validate.test_invalid_key_returns_false — fake `call_claude` raises `anthropic.AuthenticationError` → returns False.
    - test_api_key_validate.test_other_errors_propagate — fake `call_claude` raises `RuntimeError` → caller sees the RuntimeError (no swallow).
    - test_signup.test_render_signup_page_invalid_key_no_db_write — `AppTest` simulates submit with bad key (fake_client returns False); asserts no row in users + `st.error` element present.
    - test_signup.test_render_signup_page_valid_writes_row_and_switches — AppTest simulates submit with good key; asserts row present + `st.switch_page` was called with the My Classes target.
  </behavior>
  <action>
1. Write tests first (RED) under `tests/test_queries_users.py`, `tests/test_api_key_validate.py`, and `tests/test_signup.py`. Use `tests/_streamlit_apptest_helpers.py` (Plan 01) for AppTest fixtures and a fake `call_claude`. Use `:memory:` SQLite via the existing `app/db/connection.py` test seam (read connection.py to confirm how Phase 1 tests do it; mirror).
2. Implement `app/db/queries_users/queries_users.py`:
   - Match the existing query-package pattern (read `queries_classes.py` first; mirror style — `with get_db() as conn: ...`, `?` placeholders, `pd.read_sql` for reads OR `conn.execute(...).fetchone()` returning dict, whichever Phase 1 uses).
   - `save_user(username, anthropic_api_key) -> int` returns `lastrowid`.
   - `get_user_or_none() -> dict | None`.
3. Implement `app/signup/api_key_validate/api_key_validate.py`:
   - One ~10-line function `validate_anthropic_key(key) -> bool`.
   - Calls `call_claude` from `app/brain/claude_client` with `max_tokens=1` and a trivial system+user message ("ping"). Per CLAUDE.md "API key consent" memory rule, the executor MUST ask the user before this test ever fires real Anthropic calls; pytest uses the fake.
   - Catches `anthropic.AuthenticationError` (or whatever the SDK raises for invalid keys; verify by reading `claude_client.py`) → returns False. Lets other exceptions propagate.
4. Implement `app/signup/signup_flow/signup_flow.py`:
   - `render_signup_page()`: builds an `st.form(key="surf-signup", border=True, clear_on_submit=False)` with `st.text_input("Username", key="signup_username")`, `st.text_input("Anthropic API key", type="password", key="signup_api_key")`, `st.form_submit_button("Save")`.
   - On submit:
     - Show `st.spinner("Verifying with Anthropic…")`.
     - Call `validate_anthropic_key`. If False, render `st.error(...)`. Stop.
     - Else call `save_user`, `st.toast("Welcome, {username} ✅")`, then `st.switch_page("views/my_classes.py")`.
   - Wrap form in `st.container(key="surf-signup-card")` for scoped CSS targeting.
   - `is_authenticated()` returns `get_user_or_none() is not None`.
5. Implement `app/signup/signup_flow/signup_flow.md` (sidecar, <=140 lines, includes `## Code walkthrough` section per D-5.1).
6. Implement `app/signup/api_key_validate/api_key_validate.md` (sidecar).
7. Implement `app/db/queries_users/queries_users.md` (sidecar; if it already exists from Plan 01 sidecar back-fill, append walkthrough — do not duplicate).
8. Run RED→GREEN: tests should pass after implementation.
  </action>
  <verify>
    <automated>pytest -q tests/test_queries_users.py tests/test_api_key_validate.py tests/test_signup.py -x && grep -q "## Code walkthrough" app/signup/signup_flow/signup_flow.md && grep -q "## Code walkthrough" app/signup/api_key_validate/api_key_validate.md && python -c "from app.signup.signup_flow.signup_flow import render_signup_page, is_authenticated; from app.signup.api_key_validate.api_key_validate import validate_anthropic_key; from app.db.queries_users.queries_users import save_user, get_user_or_none"</automated>
  </verify>
  <done>
    - All three modules exist with sidecars (≤140 lines, walkthrough sections).
    - `pytest -q tests/test_queries_users.py tests/test_api_key_validate.py tests/test_signup.py` all green.
    - `is_authenticated()` returns False on empty DB and True after `save_user`.
    - `validate_anthropic_key` correctly handles valid / invalid / other-error paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire views/signup.py + auth router upgrade + scoped CSS for P1 + P1 sandbox</name>
  <read_first>
    - /Users/tiagoreimann/surf/streamlit_app.py
    - /Users/tiagoreimann/surf/views/signup.py
    - /Users/tiagoreimann/surf/app/brain/theme/surf_theme.css
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md (Card spec — for Card scoped styling)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md §3 (scoped CSS), §4.1 (forms)
    - /Users/tiagoreimann/surf/CLAUDE.md "Visual preview gate" section
    - /Users/tiagoreimann/surf/previews/_fixtures.py
  </read_first>
  <action>
1. Replace `views/signup.py` content with a thin wrapper:
   ```python
   from app.signup.signup_flow.signup_flow import render_signup_page
   render_signup_page()
   ```
   (Match the style of any other thin views/ file already shipped.)
2. Update `streamlit_app.py` `is_authenticated()` to delegate to `app.signup.signup_flow.is_authenticated`. SURGICAL: only the function body changes; everything below (`if is_authenticated(): pages = [...]`) stays identical. Keep the `load_theme()` call (Plan 01 wired it).
3. Add scoped CSS to `app/brain/theme/surf_theme.css` for the P1 form (per RESEARCH §3 + 02-WIDGETS.md):
   - `.st-key-surf-signup-card { background: var(--surf-paper-1); border: 1px solid var(--surf-paper-3); border-radius: var(--surf-radius-card); padding: var(--surf-space-card-lg); max-width: 480px; margin: 4rem auto; }`
   - Cite the rule with a comment `/* P1 Sign Up — D-2.4 roomy whitespace, D-2.1 Paper aesthetic */`.
4. Per D-2.x edit-this-later rule: add a row to `app/brain/theme/edit_this_later.md` for the P1 card (where it lives + what to change + propagation). One row, one line.
5. Build the P1 sandbox at `previews/pages/p1_signup/` per CLAUDE.md visual preview gate:
   - `previews/pages/p1_signup/preview.py` — entrypoint that:
     - Loads `surf_theme.css` (copied — see step 6).
     - Imports `render_signup_page` from a LOCAL `signup_flow.py` (copied — see step 7), NOT from `app/`.
     - Stubs `call_claude` with `previews._fixtures.fake_call_claude` (or a sandbox-local equivalent).
     - Uses an in-memory SQLite — provide a tiny `_memdb.py` helper inside the sandbox folder, or a closure that overrides `get_db`.
   - `previews/pages/p1_signup/signup_flow.py` — COPY of `app/signup/signup_flow/signup_flow.py` with imports rewritten to local paths. Drift is intentional.
   - `previews/pages/p1_signup/api_key_validate.py` — COPY with the stubbed `call_claude`.
   - `previews/pages/p1_signup/queries_users.py` — COPY with the in-memory connection.
   - `previews/pages/p1_signup/surf_theme.css` — COPY of the production CSS at the time of this task.
6. Build the COMPONENT sandbox at `previews/components/signup_form/`:
   - `previews/components/signup_form/preview.py` — renders the Sign Up Card in isolation (no router, no DB write) so the form's typography, spacing, and error state can be eyeballed quickly.
   - `previews/components/signup_form/signup_form.py` — minimal copy showing only the form widget tree (no validation logic).
   - `previews/components/signup_form/surf_theme.css` — COPY.
7. Smoke-check: confirm `grep -L "from app" previews/pages/p1_signup/*.py previews/components/signup_form/*.py` returns NO matches (sandbox-isolation rule).
8. Add `tests/test_auth_router.py` covering the new `is_authenticated()` delegation: empty DB → False; after `save_user` → True. (Optional AppTest assertion that `streamlit_app.py` would route to signup vs my_classes — guard with `pytest.importorskip("streamlit.testing")`.)
  </action>
  <verify>
    <automated>pytest -q tests/test_auth_router.py -x && grep -q "from app.signup.signup_flow.signup_flow import render_signup_page" views/signup.py && grep -q "is_authenticated" streamlit_app.py && grep -q ".st-key-surf-signup-card" app/brain/theme/surf_theme.css && (find previews/pages/p1_signup previews/components/signup_form -name '*.py' | xargs grep -l "^from app\\.\\|^import app\\." ; test $? -ne 0) && test -f previews/pages/p1_signup/preview.py && test -f previews/components/signup_form/preview.py</automated>
  </verify>
  <done>
    - `views/signup.py` is a thin wrapper.
    - `streamlit_app.py` `is_authenticated()` delegates to `app.signup.signup_flow.is_authenticated` (one-line surgical change).
    - `app/brain/theme/surf_theme.css` has `.st-key-surf-signup-card` rule + edit-note in `edit_this_later.md`.
    - Both sandboxes (`previews/pages/p1_signup/` + `previews/components/signup_form/`) exist; no `from app` imports anywhere under `previews/`.
    - `tests/test_auth_router.py` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tiago visually approves P1 Sign Up sandbox</name>
  <what-built>
    - A working `views/signup.py` wired to the new pipeline.
    - The P1 page sandbox (`previews/pages/p1_signup/preview.py`) with stubbed Anthropic + in-memory SQLite.
    - The P1 form component sandbox (`previews/components/signup_form/preview.py`) for typography/spacing checks.
    - Auth router upgraded.
  </what-built>
  <how-to-verify>
    1. From repo root: `streamlit run previews/components/signup_form/preview.py`
       - Confirm the form is centered, has the Paper1 background, Paper3 border, generous padding, monospace button label.
       - Confirm both fields render (username + API-key with password mask).
    2. Stop, then: `streamlit run previews/pages/p1_signup/preview.py`
       - Submit `username = tiago` + key = `sk-ant-good` → see "Welcome, tiago ✅" toast and a transition (sandbox may stop here; that's fine — the production flow does st.switch_page).
       - Refresh the browser, submit `username = tiago` + key = `sk-ant-bad` → see an inline error "Invalid Anthropic API key. Please check and retry."
       - Confirm no errors in the browser console.
    3. Optional: `streamlit run streamlit_app.py` from a clean state (no `~/.surf/user.sqlite`) — confirm P1 routes correctly. WARNING: this triggers a real Anthropic call if you enter a real key. Per CLAUDE.md API-key-consent memory, ASK before running this against the real API.
  </how-to-verify>
  <acceptance_criteria>
    - Sandbox paths: `previews/components/signup_form/preview.py` AND `previews/pages/p1_signup/preview.py`
    - Run commands:
      - `streamlit run previews/components/signup_form/preview.py`
      - `streamlit run previews/pages/p1_signup/preview.py`
    - "Tiago has visually approved the preview"
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what to fix.</resume-signal>
</task>

</tasks>

<verification>
```bash
pytest -q tests/test_queries_users.py tests/test_api_key_validate.py tests/test_signup.py tests/test_auth_router.py
grep -L "from app" previews/pages/p1_signup/*.py previews/components/signup_form/*.py
grep -q "## Code walkthrough" app/signup/signup_flow/signup_flow.md
grep -q "## Code walkthrough" app/signup/api_key_validate/api_key_validate.md
```
</verification>

<success_criteria>
- PAGE-01 + PIPE-02 closed: P1 form renders, validates key against Anthropic, persists user, routes to P2 on success.
- `is_authenticated()` reads the users table, not the file existence heuristic.
- Sandboxes exist for the page and the form component; no `from app...` imports anywhere under `previews/`.
- Sidecars for all three new pipelines exist with `## Code walkthrough` sections, ≤140 lines.
- Tiago has visually approved both sandbox previews.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-taking-loop-p1-p5/02-02-SUMMARY.md` covering: pipelines created, schema confirmation (no schema change in this plan), sandbox approvals, test coverage map (PAGE-01 + PIPE-02).
</output>