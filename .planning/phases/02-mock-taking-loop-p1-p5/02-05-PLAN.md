---
phase: 02-mock-taking-loop-p1-p5
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-04a]
files_modified:
  - views/take_mock_exam.py
  - app/mock_take/__init__.py
  - app/mock_take/question_render/__init__.py
  - app/mock_take/question_render/question_render.py
  - app/mock_take/question_render/question_render.md
  - app/mock_take/answer_capture/__init__.py
  - app/mock_take/answer_capture/answer_capture.py
  - app/mock_take/answer_capture/answer_capture.md
  - app/mock_take/attempt_save/__init__.py
  - app/mock_take/attempt_save/attempt_save.py
  - app/mock_take/attempt_save/attempt_save.md
  - app/mock_take/timer_render/__init__.py
  - app/mock_take/timer_render/timer_render.py
  - app/mock_take/timer_render/timer_render.md
  - app/db/queries_attempts/queries_attempts.py
  - app/db/queries_attempts/queries_attempts.md
  - app/db/schema/schema.sql
  - app/db/schema/schema.md
  - app/brain/theme/surf_theme.css
  - app/brain/theme/edit_this_later.md
  - tests/test_attempt_save.py
  - tests/test_question_render.py
  - tests/test_timer_render.py
  - tests/test_take_mock_apptest.py
  - previews/components/mcq_card/preview.py
  - previews/components/mcq_card/question_render.py
  - previews/components/mcq_card/surf_theme.css
  - previews/components/timer_header/preview.py
  - previews/components/timer_header/timer_render.py
  - previews/components/timer_header/surf_theme.css
  - previews/pages/p4_take_mock/preview.py
  - previews/pages/p4_take_mock/take_mock_view.py
  - previews/pages/p4_take_mock/question_render.py
  - previews/pages/p4_take_mock/answer_capture.py
  - previews/pages/p4_take_mock/attempt_save.py
  - previews/pages/p4_take_mock/timer_render.py
  - previews/pages/p4_take_mock/surf_theme.css
autonomous: false
requirements: [PAGE-04, MECH-01, GRADE-04]
must_haves:
  truths:
    - "P4 renders one MCQ at a time inside a Card with key=`mcq-card-{question_id}` (D-3.5)"
    - "MCQ card uses st.checkbox per option when len(correct_indices) >= 2 (Phase 1 D-2.5 multi-correct), st.radio otherwise (RESEARCH §2)"
    - "Difficulty score is visible on the MCQ card during taking (D-3.5); placeholder character `—` when difficulty_score IS NULL"
    - "Timer in top header bar, right-aligned, format Elapsed: MM:SS, always visible across the mock (D-3.2)"
    - "Timer uses st.fragment(run_every='1s') if Q4 verdict in 02-WIDGETS.md was PASS; else falls back to nav-driven re-render per the documented Q4 fallback"
    - "Schema constraint UNIQUE(attempt_id, question_id) is enforced on attempt_answers (D-3.3); schema.sql is amended in this plan and migrations note documents the wipe-and-rerun approach (Phase 1 D-3.1)"
    - "Navigation: PREV / SKIP / NEXT / Submit. UPSERT on every navigation event (NOT every checkbox tick) per D-3.3"
    - "SKIP records selected_indices = NULL and advances; NULL on submit grades as wrong per D-3.1"
    - "Grading is all-or-nothing: 1 point iff selected_indices == correct_indices (exact set equality), else 0 (D-3.4)"
    - "Resume: reopening P4 with an in-progress attempt picks the next unanswered question; timer continues from saved attempts.started_at (D-3.3)"
    - "Submit transitions: completes attempt (finished_at, correct_count, total_count) and st.switch_page to views/review_mock_exam.py"
    - "Sandboxes (3 paths) contain no `from app...` imports (CLAUDE.md visual preview gate); sandbox uses :memory: SQLite + stubbed claude_client"
    - "All sidecar .md files <=140 lines and include ## Code walkthrough section (D-5.1)"
  artifacts:
    - path: "app/mock_take/question_render/question_render.py"
      provides: "render_mcq_card(question: dict, prior_selection: list[int] | None) -> list[int] | None — returns user's selected indices for this render"
      exports: ["render_mcq_card"]
    - path: "app/mock_take/answer_capture/answer_capture.py"
      provides: "capture_answer(question, selected_indices) -> bool (is_correct), enforces D-3.4 exact-set equality"
      exports: ["capture_answer", "grade_attempt"]
    - path: "app/mock_take/attempt_save/attempt_save.py"
      provides: "upsert_attempt_answer(attempt_id, question_id, selected_indices) — UNIQUE constraint UPSERT"
      exports: ["upsert_attempt_answer", "complete_attempt"]
    - path: "app/mock_take/timer_render/timer_render.py"
      provides: "render_mock_timer() — fragment OR nav-driven per Q4 verdict"
      exports: ["render_mock_timer"]
    - path: "app/db/schema/schema.sql"
      provides: "amended attempt_answers table with UNIQUE(attempt_id, question_id)"
      contains: "UNIQUE(attempt_id, question_id)"
    - path: "views/take_mock_exam.py"
      provides: "P4 page wiring header (timer) + MCQ card + nav buttons + UPSERT"
      contains: "render_mcq_card"
  key_links:
    - from: "views/take_mock_exam.py"
      to: "app/mock_take/timer_render"
      via: "header bar"
      pattern: "render_mock_timer"
    - from: "views/take_mock_exam.py"
      to: "app/mock_take/question_render"
      via: "main body"
      pattern: "render_mcq_card"
    - from: "views/take_mock_exam.py"
      to: "app/mock_take/attempt_save.upsert_attempt_answer"
      via: "Next/Prev/Skip click handler"
      pattern: "upsert_attempt_answer"
    - from: "views/take_mock_exam.py"
      to: "app/mock_take/attempt_save.complete_attempt"
      via: "Submit click"
      pattern: "complete_attempt"
---

# Plan 02-05 — P4 Take Mock + Schema Constraint + Grading

## Objective

Plan 05 ships P4 Take Mock: a single-question-at-a-time UI with a top-header timer, PREV/SKIP/NEXT navigation, UPSERT-on-nav persistence, resume-on-revisit, and all-or-nothing grading. Adds the `UNIQUE(attempt_id, question_id)` constraint on `attempt_answers` (D-3.3 — currently missing from Phase 1 schema). Closes PAGE-04 and the runtime half of MECH-01.

Purpose: P4 is the most state-heavy page in Phase 2 — fragment timer + UPSERT-on-nav + multi-correct checkboxes + resume — and the locked decisions cluster D-3.1..D-3.5 only matter once this lands.

Output: `views/take_mock_exam.py`, four new pipelines (`question_render`, `answer_capture`, `attempt_save`, `timer_render`), schema amendment + migration note, three sandboxes, full TDD coverage.

## Execution context

- Workflow: `~/.claude/get-shit-done/workflows/execute-plan.md`
- Summary template: `~/.claude/get-shit-done/templates/summary.md`

## Context

- /Users/tiagoreimann/surf/CLAUDE.md
- /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (D-3.1..D-3.5)
- /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md (§2, §3, §4.3 fragment timer, §4.6 session_state, §5 anti-patterns)
- /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md (Q4 verdict + chosen approach)
- /Users/tiagoreimann/surf/.planning/phases/01-ingestion-spine-database/01-CONTEXT.md (D-2.5 multi-correct schema, D-2.6 per-option rationale, D-3.1 wipe-and-rerun)
- /Users/tiagoreimann/surf/app/db/schema/schema.sql
- /Users/tiagoreimann/surf/app/db/schema/schema.md
- /Users/tiagoreimann/surf/app/db/queries_attempts/queries_attempts.py (Plan 04)
- /Users/tiagoreimann/surf/app/db/queries_questions/queries_questions.py
- /Users/tiagoreimann/surf/views/take_mock_exam.py
- /Users/tiagoreimann/surf/previews/_fixtures.py

### Interfaces this plan creates (Plan 06 + 07 consume)

- `app/mock_take/question_render/question_render.py` — `render_mcq_card(question: dict, prior_selection: list[int] | None) -> list[int] | None`. Wraps in `st.container(key=f"mcq-card-{question['id']}")`. Returns user's current selection for this render. Uses st.checkbox per option when `len(question['correct_indices']) >= 2`, st.radio otherwise.
- `app/mock_take/answer_capture/answer_capture.py` — `capture_answer(question, selected_indices) -> bool` (is_correct, D-3.4 exact-set), `grade_attempt(attempt_id) -> dict` returns `{correct, total, note}` where `note = 5 * (correct/max) + 1`.
- `app/mock_take/attempt_save/attempt_save.py` — `upsert_attempt_answer(attempt_id, question_id, selected_indices) -> None` (uses UPSERT against UNIQUE constraint), `complete_attempt(attempt_id) -> dict` (sets finished_at, correct_count, total_count; returns the same dict as `grade_attempt`).
- `app/mock_take/timer_render/timer_render.py` — `render_mock_timer() -> None`. Either `@st.fragment(run_every="1s")` if Q4 PASS, or a plain function called once per host rerun if Q4 FAIL — chosen approach is encoded inside the function and consumed by `views/take_mock_exam.py` without conditional.

### Schema amendment

`schema.sql` currently:
```
CREATE TABLE IF NOT EXISTS attempt_answers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    attempt_id INTEGER NOT NULL,
    question_id INTEGER NOT NULL,
    selected_indices TEXT,
    is_correct INTEGER,
    answered_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY (attempt_id) REFERENCES attempts(id) ON DELETE CASCADE,
    FOREIGN KEY (question_id) REFERENCES questions(id) ON DELETE CASCADE
);
```

Amended:
```
CREATE TABLE IF NOT EXISTS attempt_answers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    attempt_id INTEGER NOT NULL,
    question_id INTEGER NOT NULL,
    selected_indices TEXT,
    is_correct INTEGER,
    answered_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY (attempt_id) REFERENCES attempts(id) ON DELETE CASCADE,
    FOREIGN KEY (question_id) REFERENCES questions(id) ON DELETE CASCADE,
    UNIQUE(attempt_id, question_id)
);
```

Migration: per Phase 1 D-3.1 wipe-and-rerun, no migration script. Document in `schema.md`: "to apply: delete `~/.surf/user.sqlite` and re-run." Add this to the existing schema.md migrations section.

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Schema amendment + answer_capture (grading) + attempt_save (UPSERT) (TDD)</name>
  <read_first>
    - /Users/tiagoreimann/surf/app/db/schema/schema.sql
    - /Users/tiagoreimann/surf/app/db/schema/schema.md
    - /Users/tiagoreimann/surf/app/db/queries_attempts/queries_attempts.py
    - /Users/tiagoreimann/surf/app/db/queries_questions/queries_questions.py
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (D-3.1, D-3.3, D-3.4)
    - /Users/tiagoreimann/surf/.planning/phases/01-ingestion-spine-database/01-CONTEXT.md (D-3.1 wipe-and-rerun)
  </read_first>
  <behavior>
    Behavior under test (RED first):
    - test_schema_unique_constraint — create fresh :memory: DB; insert (1, 1, "[0]") then attempt to insert (1, 1, "[1]") → must raise IntegrityError on the UNIQUE constraint.
    - test_attempt_save.test_upsert_first_call_inserts — `upsert_attempt_answer(1, 1, [0])` writes a row with selected_indices='[0]'.
    - test_attempt_save.test_upsert_second_call_overwrites — second call with `[1, 2]` updates the same row to selected_indices='[1, 2]' AND updates answered_at; only one row per (attempt_id, question_id).
    - test_attempt_save.test_upsert_skip_records_null — `upsert_attempt_answer(1, 2, None)` writes a row with selected_indices IS NULL (D-3.1 SKIP semantics).
    - test_attempt_save.test_complete_attempt_finalizes — complete_attempt sets finished_at, correct_count, total_count; subsequent reads of the row see those values.
    - test_answer_capture.test_exact_match_correct — `capture_answer({"correct_indices": [0, 2]}, [0, 2])` → True.
    - test_answer_capture.test_subset_wrong — `capture_answer({"correct_indices": [0, 2]}, [0])` → False (D-3.4 all-or-nothing).
    - test_answer_capture.test_superset_wrong — `capture_answer({"correct_indices": [0]}, [0, 1])` → False.
    - test_answer_capture.test_skip_null_wrong — `capture_answer(question, None)` → False.
    - test_answer_capture.test_grade_attempt_swiss — given 4 correct of 5, `grade_attempt(attempt_id)` returns `{correct: 4, total: 5, note: 5.0}` (5×4/5 + 1 = 5.0).
  </behavior>
  <action>
    Action (GREEN):

    1. Edit `app/db/schema/schema.sql`: add `UNIQUE(attempt_id, question_id)` to `attempt_answers` (one-line surgical edit).
    2. Edit `app/db/schema/schema.md`: add a "Migrations" line "Plan 02-05 added UNIQUE(attempt_id, question_id) on attempt_answers; per D-3.1 wipe-and-rerun, delete ~/.surf/user.sqlite and restart to apply."
    3. Implement `app/mock_take/attempt_save/attempt_save.py`:
       - `upsert_attempt_answer(attempt_id, question_id, selected_indices: list[int] | None) -> None`. Uses SQLite `INSERT ... ON CONFLICT(attempt_id, question_id) DO UPDATE SET selected_indices=excluded.selected_indices, is_correct=excluded.is_correct, answered_at=datetime('now')`. is_correct is computed at upsert time via `capture_answer` so P5 reads are O(1).
       - `complete_attempt(attempt_id) -> dict`: sets finished_at, correct_count, total_count via grade_attempt; returns the result dict.
    4. Implement `app/mock_take/answer_capture/answer_capture.py`:
       - `capture_answer(question: dict, selected_indices: list[int] | None) -> bool`: handles None (returns False); compares `set(selected_indices) == set(json.loads(question["correct_indices"]))`; returns bool.
       - `grade_attempt(attempt_id: int) -> dict`: SELECTs all attempt_answers for the attempt, computes correct/total/note. Use `note = round(5 * (correct / total) + 1, 2)`.
    5. Sidecars: `attempt_save.md`, `answer_capture.md`. ≤140 lines, walkthrough sections.
  </action>
  <verify>
    <automated>pytest -q tests/test_attempt_save.py tests/test_answer_capture.py -x &amp;&amp; python -c "import sqlite3, pathlib; conn = sqlite3.connect(':memory:'); conn.executescript(pathlib.Path('app/db/schema/schema.sql').read_text()); conn.execute('INSERT INTO users(username, anthropic_api_key) VALUES (1,1)'); conn.execute('INSERT INTO classes(user_id, name, factsheet_json) VALUES (1, \"x\", \"{}\")'); conn.execute('INSERT INTO attempts(class_id, mock_kind) VALUES (1, \"standard\")'); conn.execute('INSERT INTO attempt_answers(attempt_id, question_id, selected_indices) VALUES (1, 1, \"[0]\")'); import pytest; raised=False; \ntry:\n    conn.execute('INSERT INTO attempt_answers(attempt_id, question_id, selected_indices) VALUES (1, 1, \"[1]\")')\nexcept sqlite3.IntegrityError:\n    raised=True; \nassert raised" &amp;&amp; grep -q "UNIQUE(attempt_id, question_id)" app/db/schema/schema.sql</automated>
  </verify>
  <done>
    - Schema amendment lands; UNIQUE constraint enforced.
    - Test suite green.
    - All-or-nothing grading verified for exact, subset, superset, and skip cases.
    - schema.md migration note recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: question_render (MCQ card) + timer_render + P4 page wiring</name>
  <read_first>
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md (Q4 chosen approach + Card spec)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-CONTEXT.md (D-3.1..D-3.5)
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-RESEARCH.md §2 (st.checkbox/radio), §3 (scoped CSS), §4.3 (st.fragment), §4.6 (session_state), §5 anti-patterns
    - /Users/tiagoreimann/surf/views/take_mock_exam.py
    - /Users/tiagoreimann/surf/previews/_fixtures.py
  </read_first>
  <action>
    1. Implement `app/mock_take/question_render/question_render.py`:
       - `_DIFFICULTY_PENDING = "—"` constant (D-3.5 edit-this-later).
       - `render_mcq_card(question: dict, prior_selection: list[int] | None) -> list[int] | None`.
       - Wrap in `st.container(key=f"mcq-card-{question['id']}")`.
       - Inside:
         - `st.markdown(f"**{question['question_text']}**")`.
         - Difficulty score line: `st.caption(f"Difficulty: {question.get('difficulty_score') or _DIFFICULTY_PENDING}")` per D-3.5.
         - Branch on `len(json.loads(question['correct_indices'])) >= 2`:
           - Multi-correct: render one `st.checkbox(label=opt, value=(i in (prior_selection or [])), key=f"q{question['id']}_opt{i}")` per option. Collect ticks into a list of indices.
           - Single-correct: `st.radio` with `index=prior_selection[0]` if present else `None`; map back to the index.
       - Return the list of selected indices (or empty list if user has not interacted).
    2. Implement `app/mock_take/timer_render/timer_render.py`:
       - Read 02-WIDGETS.md Q4 verdict to choose:
         - PASS path: `@st.fragment(run_every="1s") def render_mock_timer(): ...` per RESEARCH §4.3 worked example.
         - FAIL path: plain function `def render_mock_timer(): ...` (re-renders only on host rerun, i.e., on nav clicks).
       - Either way: reads `st.session_state["mock_start_ts"]`; if None, returns; else renders `st.markdown(f"**Elapsed:** \`{mm:02d}:{ss:02d}\`")` inside `st.container(key="mock-timer")`.
       - Constraint: do NOT put `st.form_submit_button` or `st.dialog` inside the fragment per RESEARCH §5.
    3. Wire `views/take_mock_exam.py`:
       - Top: read `attempt_id = st.session_state.get("attempt_id")`; if None, `st.switch_page("views/class_view.py")`.
       - Initialize session_state defaults: `mock_idx` (0), `mock_start_ts` (load from `attempts.started_at` if not set; else `time.time()` and persist back via `update_attempt_started_at`).
       - Header bar: `cols = st.columns([1, 1, 1]); with cols[2]: render_mock_timer()`. With cols[0]: `st.markdown(f"**Question {idx+1} of {N}**")`.
       - Load the attempt's question list: `question_ids = [row["question_id"] for row in get_attempt_answer_rows(attempt_id)]`. Ordering = the order they were inserted in Plan 04.
       - Resume rule: on page load (idx not yet set this session), set `mock_idx` to the first row whose selected_indices is NULL — D-3.3.
       - Body: `q = get_question_by_id(question_ids[mock_idx])` ; render via `render_mcq_card(q, prior_selection=...)` ; capture user's current selection.
       - Nav row (`st.columns(4)`): PREV (disabled when idx==0), SKIP, NEXT (disabled when idx==N-1), Submit (always shown; inline confirm if any selected_indices is NULL):
         - PREV: `upsert_attempt_answer(attempt_id, q['id'], current_selection); idx -= 1; st.rerun()`.
         - SKIP: `upsert_attempt_answer(attempt_id, q['id'], None); idx += 1; st.rerun()`.
         - NEXT: `upsert_attempt_answer(attempt_id, q['id'], current_selection); idx += 1; st.rerun()`.
         - Submit: `upsert_attempt_answer(attempt_id, q['id'], current_selection); summary = complete_attempt(attempt_id); st.session_state["attempt_id"] = attempt_id; st.switch_page("views/review_mock_exam.py")`.
       - Submit gating per CONTEXT Discretion: "always visible, with a confirm dialog when unanswered questions exist". Phase 2 has no `st.dialog` per RESEARCH §11 Q5; instead use a simple inline `st.warning("X questions unanswered — these will count as wrong. Continue?") + Yes/No buttons` if NULL count > 0.
    4. Add scoped CSS to `app/brain/theme/surf_theme.css` for the MCQ card and the timer header per D-3.2 + D-3.5:
       - `.st-key-mcq-card { background: var(--surf-paper-1); padding: var(--surf-space-card-lg); border-radius: var(--surf-radius-card); ... }`.
       - `.st-key-mock-timer { text-align: right; font-family: var(--st-code-font); ... }` (RESEARCH §4.3 snippet).
       - Hide Streamlit sidebar on P4 to maximize MCQ focus per RESEARCH §2.2 note: `[data-testid="stSidebar"]` overridden via a P4-specific class set by `st.set_page_config` or a body-level scoped class. SAFER: leave the sidebar visible in Phase 2; do not rely on `data-testid` for layout-critical paths. Document this trade-off in the plan summary.
    5. Add edit-this-later notes for: timer placement (D-3.2), difficulty placeholder (D-3.5), multi-correct grading (D-3.4), MCQ card layout.
    6. Sidecars: `question_render.md`, `timer_render.md`, append to existing as needed.
  </action>
  <verify>
    <automated>pytest -q tests/test_question_render.py tests/test_timer_render.py tests/test_take_mock_apptest.py -x &amp;&amp; python -c "from app.mock_take.question_render.question_render import render_mcq_card; from app.mock_take.timer_render.timer_render import render_mock_timer; from app.mock_take.attempt_save.attempt_save import upsert_attempt_answer, complete_attempt" &amp;&amp; grep -q "st-key-mcq-card" app/brain/theme/surf_theme.css &amp;&amp; grep -q "st-key-mock-timer" app/brain/theme/surf_theme.css &amp;&amp; grep -q "Code walkthrough" app/mock_take/question_render/question_render.md app/mock_take/timer_render/timer_render.md</automated>
  </verify>
  <done>
    - Three new test files green (question_render, timer_render, take_mock_apptest covering navigation + UPSERT).
    - `views/take_mock_exam.py` is wired with header / body / nav row.
    - All-or-nothing grading runs end-to-end through the AppTest harness.
    - Scoped CSS added; edit-this-later rows recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build sandboxes for MCQ card, timer header, and full P4 page</name>
  <read_first>
    - /Users/tiagoreimann/surf/CLAUDE.md "Visual preview gate" section
    - /Users/tiagoreimann/surf/previews/_fixtures.py
    - /Users/tiagoreimann/surf/.planning/phases/02-mock-taking-loop-p1-p5/02-WIDGETS.md (Q3 + Q4 chosen approaches)
  </read_first>
  <action>
    1. `previews/components/mcq_card/`:
       - `preview.py` shows three MCQ cards using FAKE_MCQS:
         - One single-correct (radio).
         - One multi-correct (checkboxes) per Phase 1 D-2.5.
         - One with `difficulty_score = None` to verify the `—` placeholder (D-3.5).
       - `question_render.py` is a copy of `app/mock_take/question_render/question_render.py`, no `from app` imports.
       - `surf_theme.css` is a copy.
    2. `previews/components/timer_header/`:
       - `preview.py` boots a 1s fragment timer (or nav-driven fallback per Q4) and renders a Reset button. Pure visual probe.
       - `timer_render.py` is a copy.
       - `surf_theme.css` is a copy.
    3. `previews/pages/p4_take_mock/`:
       - Boots :memory: SQLite, seeds an in-progress attempt with 5 questions (3 unanswered, 1 skipped, 1 answered) using `_fixtures.FAKE_MCQS`.
       - `preview.py` calls `take_mock_view.render_page()`.
       - Files: `take_mock_view.py`, `question_render.py`, `answer_capture.py`, `attempt_save.py`, `timer_render.py`, `surf_theme.css` — all copies.
       - `attempt_save.py` runs against the in-memory DB; UPSERT works exactly as production.
       - No `from app` imports anywhere.
    4. Smoke check: `find previews/components/mcq_card previews/components/timer_header previews/pages/p4_take_mock -name '*.py' | xargs grep -l "^from app\\|^import app"` returns no matches.
  </action>
  <verify>
    <automated>python -c "import ast, pathlib; [ast.parse(p.read_text()) for p in pathlib.Path('previews').rglob('*.py')]" &amp;&amp; bash -c 'set +e; out=$(find previews/components/mcq_card previews/components/timer_header previews/pages/p4_take_mock -name "*.py" -print0 | xargs -0 grep -l "^from app\|^import app"); test -z "$out"'</automated>
  </verify>
  <done>
    - Three sandboxes exist; valid Python; no `from app` imports.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Tiago visually approves P4 sandboxes</name>
  <what-built>
    - `views/take_mock_exam.py` end-to-end with timer header, MCQ card, nav row, UPSERT-on-nav, resume-on-revisit, and submit-with-warning.
    - Schema amendment + UPSERT helper + grading function.
    - Three sandboxes covering MCQ card, timer header, and full P4 page.
  </what-built>
  <how-to-verify>
    1. `streamlit run previews/components/mcq_card/preview.py`
       - Confirm: difficulty score visible (or `—`), question text bold, options laid out with monospace labels, multi-correct uses checkboxes, single-correct uses radio.
    2. `streamlit run previews/components/timer_header/preview.py`
       - Confirm: timer ticks every 1s (PASS path) or updates only on Reset click (FAIL path); right-aligned in monospace.
    3. `streamlit run previews/pages/p4_take_mock/preview.py`
       - Walk through 5 questions:
         - Skip Q1 → confirm Q2 has the prior selection if revisited via Prev.
         - Multi-correct on Q3 with one option missing → Submit warns "1 unanswered — counts as wrong"; confirm proceeds → review screen shows 0 for Q3.
       - Confirm timer keeps ticking across PREV/NEXT (does NOT reset).
       - Confirm refreshing the browser resumes at the next unanswered question and the timer continues from saved start.
    4. NEVER run `streamlit run streamlit_app.py` and submit a real mock here without explicit user consent — the production path writes to `~/.surf/user.sqlite`. Per CLAUDE.md API-key-consent memory and surgical-changes rule, ASK before triggering.
  </how-to-verify>
  <acceptance_criteria>
    - Sandbox paths created/updated:
      - `previews/components/mcq_card/`
      - `previews/components/timer_header/`
      - `previews/pages/p4_take_mock/`
    - Run commands:
      - `streamlit run previews/components/mcq_card/preview.py`
      - `streamlit run previews/components/timer_header/preview.py`
      - `streamlit run previews/pages/p4_take_mock/preview.py`
    - "Tiago has visually approved the preview"
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what to fix.</resume-signal>
</task>

## Verification

- `pytest -q` (no Phase 1 tests regressed; Plan 05 tests green).
- `grep -q "UNIQUE(attempt_id, question_id)" app/db/schema/schema.sql`.
- All three sandboxes runnable; no `from app` imports.
- All sidecars ≤140 lines, walkthrough sections present.

## Success criteria

- PAGE-04 closed: P4 renders one MCQ at a time, timer in header, PREV/SKIP/NEXT/Submit nav, UPSERT-on-nav, resume.
- MECH-01 closed (runtime half): all-or-nothing grading verified; mock state persisted to DB on every nav event.
- MECH-03 closed (formula): `note = 5 × correct/max + 1` returned from `complete_attempt`; tested at exact and edge values.
- Schema constraint UNIQUE(attempt_id, question_id) is in `schema.sql` and migration note in `schema.md`.
- Tiago has visually approved all three P4 sandboxes.

## Output

Create `.planning/phases/02-mock-taking-loop-p1-p5/02-05-SUMMARY.md` covering: schema amendment + migration approach, Q4 timer path chosen, multi-correct grading verification, sandbox approvals, sidecar status.
